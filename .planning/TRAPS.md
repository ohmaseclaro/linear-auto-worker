# Known Traps Ledger

Running list of framework/tool footguns discovered during the milestone. Passed into every
planning and execution brief so no two waves rediscover the same trap independently.

Seeded 2026-09-06 from `.planning/research/SUMMARY.md` "Corrections to PROJECT.md" — these were
verified against live tools/registries during project research, not recalled from training data.

## Seeded (verified before any code existed)

| # | Trap | Failure mode | Correct move | Phase |
|---|------|--------------|--------------|-------|
| T1 | `claude -p` starts in **Manual** permission mode | Every edit denied, process still **exits 0** — looks like success, produces nothing | Always pass an explicit `--permission-mode` | 4 |
| T2 | `claude -p --bare` skips `~/.claude` | Kills the global GSD install the spawned agent depends on; fails silently | `--bare` is forbidden, no exceptions | 4 |
| T3 | `--output-format stream-json` without `--verbose` | Hard startup error, not a warning: "requires --verbose" | Always pair the two flags | 4 |
| T4 | Parsing session id out of the stream | Race-prone — many `hook_started` events precede `system/init` | Pre-assign `--session-id <crypto.randomUUID()>`, persist before spawn | 4 |
| T5 | `@linear/sdk` v93 **removed** the `LinearWebhooks` class | Any code written against the old class does not compile | `LinearWebhookClient` + `createHandler()` from `@linear/sdk/webhooks` | 3 |
| T6 | Invalid webhook signature returns **400**, not 401 | A 401 assertion in tests never fires | Assert 400 | 3 |
| T7 | Linear rate limiting is **HTTP 400** + `errors[].extensions.code === "RATELIMITED"` | Any `status === 429` branch is dead code that never runs | Match on the extension code, back off per the reset header | 5 |
| T8 | `@linear/sdk` ships a new **major roughly weekly** (86 to 93 in 15 weeks) | `^93` or `~93` guarantees an eventual silent breaking bump | Pin exact `93.0.1`, bump deliberately | all |
| T9 | Personal API key auth header has **no `Bearer` prefix** | 401 on every call | `Authorization: <KEY>` | 5 |
| T10 | `@ngrok/ngrok` does **not** read `NGROK_AUTHTOKEN` automatically, nor `~/.config/ngrok/ngrok.yml` | Tunnel fails to open with an auth error despite the env var being set | Pass `authtoken_from_env: true`; wizard lifts the token out of the YAML | 3, 8 |
| T11 | An MCP long-poll `ask_human` tool **does not block** the agent | Claude Code backgrounds a main-conversation MCP call past ~2 min; agent proceeds without the answer | Exit-and-`--resume` is the only reliable Q&A mechanism | 4, 6 |
| T12 | `typescript@latest` is now **7.0.2** (the Go-native `tsgo` rewrite) | Type-checking behaviour gaps against `@linear/sdk`'s ~74k-line `.d.ts` | Pin `~5.9` | 1 |
| T13 | `gh pr create` has **no `--json` flag** | Command errors out | Read the URL from stdout; `gh pr view --json` afterwards | 4 |
| T14 | Body-parsing before HMAC verification destroys the byte-exact raw body | Re-stringifying a parsed body produces signature mismatches (key order, unicode escaping) | No HTTP framework; let the SDK handler consume the raw stream | 3 |
| T15 | This machine runs **Node v22.23.1**; STACK.md recommends Node 24 (Active LTS) | Not fatal — `better-sqlite3@13` and `execa@10` both require only `>=22` — but the wizard's own preflight will flag the operator's box | Build against >=22; decide in Phase 8 whether the preflight hard-fails or warns on <24 | 1, 8 |

## Verified clean (do not "fix" these)

- **2026-09-06 — the entire pinned dependency set installs cleanly.** Verified by a real
  `npm install` in a scratch directory: 87 packages, 11 s, every pin resolving exactly as
  written in STACK.md. `typescript@~5.9` resolves to **5.9.3** (correctly not the TS7
  `tsgo` rewrite), `@linear/sdk@93.0.1`, `better-sqlite3@13.0.3` shipping a prebuilt
  `darwin-arm64.node` (no node-gyp, no Xcode CLT needed), and `@ngrok/ngrok-darwin-arm64`
  present via optionalDependencies. **Do not bump any pin.** If an install fails, the
  cause is local, not the version.

## Discovered during execution

| # | Trap | Failure mode | Correct move | Phase |
|---|------|--------------|--------------|-------|
| T16 | **`research/ARCHITECTURE.md`'s transition table predates the binding contract and uses STALE state names** — `claimed`, `worktree_ready`, `agent_running`, `done`, `abandoned`. None are in the locked nine. | Code written against them compiles in isolation and fails only at the final integration gate, with no attribution to a phase. Under rush mode nothing catches it earlier. | The binding nine in `01-CONTEXT.md`'s ADDENDUM win, always. Translation: `claimed`→`queued`, `worktree_ready`→`preparing`, `agent_running`→`running`, `done`→`delivered`\|`partial`, `abandoned`→`cancelled`\|`failed`. | all |
| T17 | **The same table encodes a bounded `failed → queued` auto-retry.** | Directly contradicts OPS-04 and 06-CONTEXT D-13 — a failed run is attempted exactly once. Building the retry produces duplicate PRs and burns API budget. | No retry machinery anywhere. Failure posts a diagnosis and leaves the branch and worktree for the operator. | 6 |
| T18 | **`research/SUMMARY.md` Invariant 7 says requeue every `running` row found at boot; 06-CONTEXT D-07 says fail it.** | Requeueing blind re-runs a ticket whose push status is unknowable, producing a second PR for work already pushed. | D-07 wins. Phase 7's clean shutdown marks children `queued` before exit, so a `running` row surviving to boot means an *unclean* exit — exactly the case that must not be replayed. | 6, 7 |
| T19 | **`listener.url()` returns `string \| null`** on `@ngrok/ngrok` | Unchecked, the reconciler registers `"null/linear/webhook"`. Daemon boots clean, reports healthy, and receives nothing — the worst failure shape in the project. | Assert non-null before registration; fail boot loudly. | 3 |
| T20 | **`webhookTimestamp` is MILLISECONDS** (proven twice: schema doc comment + live 200/400 test). The ±60s staleness check is **already in the SDK** — do not hand-roll it. | The SDK's check is wrapped in `if (timestamp)`, so an **absent** field silently skips replay protection and returns 200. Always-pass, looks like working code. | Do not reimplement the window; DO assert the field is present. | 3 |
| T21 | **`client.webhookCreate` does not exist** (confirmed by `tsc` TS2339) — those are GraphQL mutation names, not SDK methods. | Compile error at best; hours lost guessing at worst. | `createWebhook` / `updateWebhook` / `deleteWebhook`. D-02's re-enable is one call: `updateWebhook(id, {url, enabled: true})`. | 3 |
| T22 | **`fetchNext()` mutates and returns `this`**, appending into `page.nodes` | The intuitive pagination loop double-counts every page, so the reconciler classifies its own live webhook as a duplicate and **deletes it**. `PITFALLS.md`'s reconcile snippet does not paginate at all, so this gets written fresh with no guard. | Do not accumulate across `fetchNext()` calls — read `page.nodes` once after exhausting. | 3 |
| T23 | **Every `webhooks()` call pulls signing secrets into memory** (`WebhookFragment` selects `secret`, typed `string \| null \| undefined`) | One `log.info({webhooks})` in the reconciler leaks every signing secret to disk. | Never log a webhook object. The logger's global redaction list (02-CONTEXT D-05) must cover it. | 3, 2 |
| T24 | **ngrok's malformed-token error echoes the token back in its message** | `log.error(err)` writes the operator's authtoken to disk. | Never log a raw ngrok error object. Extract and log the code only. | 3 |
| T25 | **All ngrok errors carry `code: "GenericFailure"`** | `switch (err.code)` is dead code that never discriminates. Also: `authtoken_from_env: true` with the var unset fails *identically* to no credential, so TUN-03's "clear message" needs its own pre-check. | Discriminate on `ERR_NGROK_4018` / `ERR_NGROK_105` in the message text. | 3 |
| T26 | **Clean shutdown must mark in-flight runs `queued`, not leave them `running`.** The Phase 7 planner read 07-CONTEXT D-03 ("in-flight runs are marked") together with 06-CONTEXT D-07 ("`running` at boot fails with a diagnosis") and concluded a cleanly-stopped run gets *failed* on the next boot. | That conclusion is right given the two documents alone, and it makes Ctrl-C cost a manual restart of every live run. | Both decisions are correct **once shutdown writes the state**: a clean stop transitions `running`/`delivering` → `queued` before exit, so those rows requeue. D-07's fail-on-`running` then applies only to an **unclean** exit — exactly the case where push status is unknowable and replaying would produce a second PR. This is what T18 already asserts; the Phase 7 planner started before T18 was committed. Plan 07-05 must implement the transition, not just a log line. | 7, 6 |
| T27 | **`--permission-mode dontAsk` ALONE denies the `Write` tool and exits 0.** Measured: `decision_reason_type: "mode"`, nothing created, `is_error: false`, `subtype: "success"`. | Trap T1 verbatim — the exact silent-nothing failure Phase 4 exists to prevent, reached via the flag chosen to avoid it. | `dontAsk` **plus** `--allowedTools "Write" "Edit" "Bash"` — verified zero denials and a real git commit. Never `dontAsk` alone. | 4 |
| T28 | **Inherited `CLAUDE*` env vars break the spawned agent.** The same probe run from *inside* a Claude session (22 inherited vars) denied `Write` with `decision_reason_type: "asyncAgent"` — "no approval surface". | Permission behaviour depends on how the operator launched the daemon. Deleting two known keys is not enough. | Build the child env by **allowlist**, not denylist. This supersedes 04-CONTEXT D-15's two-key scrub. | 4 |
| T29 | **`SIGINT` to the process group kills only the leader.** Empirical matrix: `detached:true` + `process.kill(-pid,"SIGTERM")` reaped child *and* grandchild; SIGINT did not. `child.kill()` even when detached orphans the grandchild **and leaves the promise permanently pending**, because the survivor holds the stdout pipe. | A run that appears hung forever, with an orphaned agent still writing. | Escalation with liveness checks between steps is mandatory, not a courtesy. SIGINT-first is still correct for resumability but must not be relied on to reap. | 4 |
| T30 | **execa 10 has no `.on()`.** `PITFALLS.md`'s Pitfall-5 sample is `node:child_process` code. | Pasting it onto an execa handle crashes. | Use execa's own API surface; do not copy the pitfall snippet verbatim. | 4 |
| T31 | **Read `result.structured_output` (parsed object), not `result.result` (string)** for `--json-schema` output. And a resumed session's `stop_reason` becomes `"tool_use"`. | Any `stop_reason === "end_turn"` check is a bug that fires on every resumed session — i.e. on every answered question. | Verified: `--json-schema` DOES survive `--resume`, and the resumed session correctly recalled the prior turn. The D-16 Q&A design holds. | 4, 6 |
| T32 | **The self-event marker constant lives in `src/domain/`, NOT in an ingress- or shared-specific module.** Canonical symbols: `BOT_COMMENT_MARKER_PREFIX`, `QUESTION_MARKER_PREFIX`, `isBotAuthoredBody`, reachable via the `src/domain/index.ts` barrel. | Two phases guessed independently and differently — Phase 3 was briefed toward `src/shared/markers.ts`, Phase 5's planner assumed `../../ingress/self-event-marker.js` with a different symbol name. Either guess produces a second copy of the constant, which is exactly what research says makes the loop-prevention filter unmergeable. | Import from the domain barrel. Never declare, re-export, or re-derive it. Both planners have been corrected. | 3, 5 |
| T33 | **Phase 1 and Phase 2 both write `package.json` and both write a five-table migration.** Confirmed on main: `02-01` landed a partial `package.json` (3 deps, 1 dev, **no `scripts` block at all**, so no `verify` command) plus `src/infra/store/migrations/001-init.ts`. | Whichever merges second either clobbers the other or leaves two schema definitions that drift. Losing the `verify` script silently removes the milestone's only quality gate. | **Ownership is settled: Phase 1 owns `package.json` and the schema + `PRAGMA user_version` runner (01-CONTEXT D-10/D-11); Phase 2 owns `db.ts` — the connection, WAL, and busy timeout.** On merge conflict, Phase 1's `package.json` wins outright (its pinned set is a superset per STACK.md and it carries the `verify` script); confirm no Phase 2 dependency is dropped. Phase 2's migration file is superseded — it must import Phase 1's rather than define its own. | 1, 2, 7 |
| T34 | **The canonical verify command `tsc --noEmit && node --test` runs ZERO tests and fails everywhere.** Verified on this machine: `node --test` cannot resolve a `.js` specifier inside a `.ts` file (`ERR_MODULE_NOT_FOUND`); the identical import written `'./lib.ts'` passes. Node's type stripping does not remap extensions. | The whole milestone writes NodeNext `.js` specifiers, so the milestone's ONLY quality gate is broken by construction — and under rush mode nothing else would have caught it. | **RESOLVED (v2 — v1 was wrong, see T55): `tsc && node --test "dist/**/*.test.js"`** — compile first, run the compiled output. Chosen over flipping to `.ts` specifiers + `allowImportingTsExtensions` because the `.js` convention is contract text eight branches have already written; changing the script is one line, changing the convention is eight branches. Phase 7 applies it. | 1, 7, all |
| T35 | **`erasableSyntaxOnly: true` is in tsconfig, and it binds every branch.** | No `enum`, no `namespace`, and no constructor parameter properties (`constructor(private x: T)`) anywhere in `src/`. Node's type stripping only accepts the erasable subset; a violation compiles under some settings and fails to load at runtime. | Use plain `const` objects with `as const` instead of enums, and explicit field assignment instead of parameter properties. | all |
| T36 | **`better-sqlite3@13.0.3` ships no type declarations** — TS7016 under `strict`. | Breaks plan 01-04 and all of Phase 2. Easy to misdiagnose as unfixable, because STACK.md's *version table* omits `@types/better-sqlite3`. | It IS in STACK.md's `## Installation` block, and was inside the verified-clean 87-package set all along. **Read the Installation block, not just the table.** | 1, 2 |

| T85 | **`node --test` cannot resolve a `.js` relative specifier inside a `.ts` file.** Verified on this machine (Node v22.23.1): `import './lib.js'` from a `.ts` file fails `ERR_MODULE_NOT_FOUND`; the identical import written `'./lib.ts'` passes. | D-13's locked gate `tsc --noEmit && node --test` runs **zero** tests and reports failure for every phase, because the whole milestone writes NodeNext `.js` specifiers. Under rush mode nothing catches this until the integration gate, where it looks like a mass test failure rather than a resolution problem. | Keep writing `.js` specifiers — they are binding contract text on all eight branches and 01-01's own gate asserts them. Phase 7 owns the fix when it extends the gate: either (a) build first and run `tsc && node --test dist`, or (b) flip source specifiers to `.ts` plus tsconfig `allowImportingTsExtensions` + `rewriteRelativeImportExtensions` (a mechanical repo-wide rewrite). Do **not** change the `verify` string on a phase branch. | 7, all |
| T86 | **`better-sqlite3@13.0.3` ships no type declarations** — registry metadata has neither a `types` nor a `typings` field. Easy to miss because STACK.md's **table** does not mention it; only the `## Installation` code block does. | Every `import Database from 'better-sqlite3'` fails `tsc --noEmit` with TS7016 under `strict`. Bites plan 01-04's migration runner and all of Phase 2's store. | `@types/better-sqlite3` is required and **is** part of STACK.md's verified-clean install line, so it is in scope, not a new dependency. Added by 01-01 at `^9` (registry latest 9.6.0; STACK.md pins no version, so the verified install resolved to the same 9.x). Read the Installation block, not just the table, before concluding a package is unsanctioned. | 1, 2, 7 |
| T37 | **A stock Linear workspace has BOTH "In Progress" and "In Review" typed `started`.** | Resolving the In Progress state by `type === 'started'` with first-match-wins non-deterministically parks a freshly picked-up ticket into **In Review**. Intermittent, workspace-dependent, and looks like the bot misbehaving rather than a resolution bug. | Filter by type, then `sort` by `position` and take the lowest. Never first-match. | 5 |
| T38 | **`ARCHITECTURE.md`'s port sketch diverges from what the Linear SDK actually needs** — `setIssueState` requires `teamId`; `review` is not a Linear state type; `createWebhook` returns `{ id }` only. | A `ports.ts` written to the sketch fails `tsc` at the integration gate. | Two phases independently reported `LinearIssue.teamId` missing (06-01 and 05-01). It is folded into plan 01-02. | 1, 5 |
| T39 | **ngrok v3 on darwin writes its config to `~/Library/Application Support/ngrok/ngrok.yml`, NOT the XDG path** that 08-CONTEXT D-05 names. **Verified on this machine: the XDG path does not exist; the darwin path exists and contains an `authtoken` line.** | The wizard's token-lift finds nothing and prompts the operator for a credential they already have — on the operator's own machine, at first run. | Check both paths. Combined with T10 (the SDK reads only the environment, never the YAML), the wizard must read the value out and copy it into `.env`. | 8 |
| T40 | **`src/domain/` must export `CONFIG_ROOT` / `CONFIG_PATH` / `ENV_PATH` / `DB_PATH`.** The ADDENDUM fixes `~/.linear-auto-worker/` as prose but names no symbol. | Plan 08-05, the daemon, and the store each derive the `.env` and database paths independently, and one of them will differ. | Fold the four constants into plan 01-02. | 1, 2, 8 |
| T41 | **Every test file must live INSIDE `src/`.** `tsconfig` is `rootDir: "src"` / `include: ["src"]` and verify is `tsc && node --test dist`. | A test written to `test/` never compiles into `dist`, so `node --test dist` never runs it. Green gate, zero coverage — the same silent-success shape as T34, and just as invisible. | Co-locate tests beside their source as `src/**/*.test.ts`. Verified: all current tests already comply. | all |
| T42 | **`src/infra/logger.ts` shipped a constructor parameter property**, violating T35. Found by Phase 3's executor reading a *merged* file it did not own. | Would have failed `tsc` at the integration gate, attributed to nobody. | **Fixed on main by the orchestrator** — explicit field assignment, with a comment naming the reason. A repo-wide scan confirmed it was the only one; no enums or namespaces anywhere. | 2 |
| T43 (UPGRADED — this one is LOUD, not silent) | **Tier-1 answer correlation was dead code, and tier 2 masked it.** `openQuestion` never posted the question comment nor stored its returned id — 06-01 left `linearCommentId` null and no other plan claimed the write — so `parentId === linearCommentId` could never match. | The top-level fallback then answered every single-question ticket, so the threaded design silently degraded to "accept any comment" while grepping clean and passing review. The failure only shows on a ticket with two open questions. | Fixed in 06-03: `openQuestion` posts the comment and stores the id; `QuestionsDeps` gained `linear: LinearClient`, which **Phase 7's composition root must wire**. **Good news found at 07-01: this surfaces as a hard type error at three call sites, so it cannot ship unwired.** Moves from the silent-failure pile to the cheap one. A tier-2 fallback that can mask tier 1 needs a test with N>1 open questions, not N=1. | 6, 7 |
| T44 | **`maxQuestionRounds` is unenforced — an agent can ask questions indefinitely.** Not covered by QA-02/04/05/06/07, so no plan owns it. | A stuck agent loops question→answer→question, consuming a Linear comment and an operator interruption each round, with no bound. | Flagged as a stub in 06-03. Decide at Phase 7 whether to enforce a cap or accept it; do not let it merge as an unnoticed gap. | 6, 7 |
| T45 | **`DomainEvent` collision — the daemon would boot clean and process nothing.** Phase 6's merged `run-engine.ts` switches on `run.requested`/`question.answered`/`run.cancelled`/`ignored`; Phase 3's router emits `issue.assigned`/`issue.unassigned`/`comment.created`. **Zero overlap.** | Ingress emits events the engine drops through `default:`. Boots clean, verifies clean, does nothing — and once the union contains both sets a typecheck cannot see it. This is the exact class of break the fan-out risks and no per-phase gate would catch. | **Union BOTH kind sets in `ports.ts`** (plan 01-02, instructed). Do NOT rename Phase 3's kinds — three of its gated verify strings assert them literally. The ingress→engine **mapping step is Phase 7's**, at the same seam where D-05's `parentId` correlation lives. | 1, 3, 6, 7 |
| T46 | **`kv` accessor naming disagrees three ways on main**: `kvGet` (8 uses), `kvSet` (3), `kvPut` (3). | Two of the three fail to resolve at integration. | **Settled: `kvGet` / `kvSet`**, matching the actual `sqlite-store.ts` implementation. The three `kvPut` call sites get renamed. Same for `recordDelivery` (implemented) over `tryInsertDelivery` (requested). | 1, 2, 3, 7 |
| T47 | **Timestamp columns are declared `TEXT` but the code writes epoch-ms numbers.** SQLite's dynamic typing accepts it, and ordering works *only* because ms timestamps are currently a fixed digit width. | An undocumented invariant holding up the deadline sweep and every `ORDER BY`. Comparisons between TEXT and INTEGER affinity are a further hazard. | Declare `created_at`, `updated_at`, `asked_at`, `deadline_at`, `received_at`, `at` as **`INTEGER`** in plan 01-04's migration. Type them as numbers in the domain types. | 1, 2 |
| T48 | **`logger.ts`'s `redact()` has no cycle guard** — a truly circular object stack-overflows. | Found by 02-02, out of its file scope, deliberately not fixed. Its test is expected to FAIL when the integration gate first runs. | Fix at Phase 7, or accept and delete the test. Do not let a known-failing test be mistaken for a regression at the gate. | 2, 7 |
| T49 | **Loop guard 3 is only half-wired.** `noteSelfWrite` is called by the notifier after each comment write, but **`setIssueState` — Phase 6's write through `LinearClient` — has no call site.** Signature confirmed synchronous: `noteSelfWrite(entityType: string, entityId: string): void`, so the notifier's sync call is correct. | The self-write suppression window never sees state transitions, so the bot's own In Progress transition can re-enter as an unfiltered event. Three guards still catch it, which is exactly why this stays invisible. | Add `noteSelfWrite('Issue', issueId)` at the `setIssueState` call site. Phase 7 owns it — no phase's plan covers that seam. | 6, 7 |
| T50 | **A boot-recovered failed run posts NOTHING to Linear.** `run-engine`'s `announceTerminal` and `fail` are both private and are emitted from the driver's `finally` — but a run recovered at boot has no driver. | `running` → `failed` at boot is correct in SQLite and in `run_events`, and completely silent on the ticket. The operator is left with a ticket stuck In Progress and no explanation — **the exact failure shape 06-CONTEXT D-08 exists to prevent.** | Expose `announceTerminal` (or `fail`) on the `RunEngine` interface and call it from recovery. One line. | 6, 7 |
| T51 | **Two competing missed-work implementations are both merged.** `recovery.reconcile()` (06-04) consumes and persists the watermark; `ingress.pollForMissedWork()` (03-04) returns `{events, watermark}` and persists nothing. Both read `poll_watermark`, both cover D-04 and D-05. | Wiring both double-processes; wiring neither makes D-08's null-actor drop unsound, since that drop is only safe *because* the poll exists. | **Phase 7 wires exactly one.** Decide deliberately and delete or leave the other explicitly unwired with a comment. | 3, 6, 7 |
| T52 | **Integration-debt baseline (CORRECTED). The first measurement of 46 was itself taken under the T54 syntax mask and is unreliable. True count with 20/35 plans merged: 56 errors.** By file: `run-engine.ts` 16, `questions.ts` 6, test files 13, `registrar.ts` 3, `scheduler.ts` 2, others 6. By code: TS2339 ×12 (property missing), TS2345 ×10 (arg type), TS2322 ×5, TS2307 ×5 (unresolved module — branches not yet merged), TS7006 ×4. | Useful as a size estimate for Phase 7, not as a target: the number will move as remaining branches merge. | Most are the known R1–R11 items — `config.defaults.concurrency` → `config.concurrency`, and `Run.state` becoming `RunState \| null`. Re-measure after the final merge. | 7 |
| T53 (**CONFIRMED EMPIRICALLY** 2026-09-06 — ran the real migration in an in-memory better-sqlite3 and diffed against the store's SQL) | **Phase 2's merged `sqlite-store.ts` fails at RUNTIME against Phase 1's authoritative schema — and `tsc` reports ZERO errors for it.** Real schema: `deliveries(delivery_id, received_at)`, `kv(k, v, updated_at)`. Store queries: line 262 `INSERT OR IGNORE INTO deliveries (id, received_at)`, line 272 `SELECT value FROM kv WHERE key = ?`, line 279 `INSERT INTO kv (key, value) ... ON CONFLICT(key)`. Three statements, none of those columns exist.** TypeScript does not know SQL column names, so the typecheck passes and every store call throws on first execution. Four divergences: `deliveries.id` → `delivery_id`; `kv.key`/`kv.value` → `k`/`v` plus `updated_at`; `receivedAt: number \| string` → number; `assumption` nullable → NOT NULL. Plus `db.ts`'s own `Migration`/`runMigrations` are superseded by `migrate.ts`. | The single most dangerous break of the milestone so far, because it is invisible to the only automated gate that exists. A green typecheck here means nothing. | **FIXED 2026-09-06 by the orchestrator, after 07-02 reached tsc exit 0 — the green typecheck did not surface it.** Applied: line 262 `id` → `delivery_id`; lines 272/279 `key` → `k`, `value` → `v`, and add `updated_at`. Failure points are the webhook dedupe on the FIRST delivery and the kv read on the FIRST boot — where the tunnel URL and webhook secret live, so the daemon dies at startup. **The integration gate must actually execute a store round-trip, not just compile.** 01-04 kept the export name `migration001` so `db.ts` still resolves unmodified. | 2, 7 |
| T54 | **A backtick inside a SQL comment closes the TypeScript template literal holding the schema.** `001-init.ts` line 124 read ``-- in `detail`, and a sixth column...`` — those backticks terminated the template string, and the rest of the comment parsed as JavaScript. | Three compounding failures: (1) the file did not compile; (2) **a syntax error aborts parsing, which suppressed 53 downstream type errors** — the reported count fell from 46 to 3 and *looked like progress*; (3) the executor's own `node:sqlite` check passed, because it validated the **SQL**, not the TypeScript wrapper around it. | Never use backticks in a SQL comment inside a template literal — D-11 makes migrations TS modules, so every SQL comment is inside one. **And never trust a falling error count without first grepping for TS1005/TS1128/TS1002; a syntax error masks everything after it.** Fixed on main by the orchestrator. | 1, all |
| T55 | **`node --test dist` runs ZERO tests on Node 22** — it treats the directory as a module to execute and dies with `MODULE_NOT_FOUND`. This was the orchestrator's own first fix for T34, and it was wrong. | Measured on this machine with a 2-test probe (1 passing, 1 deliberately failing): `node --test dist` → "1 test, 0 pass, 1 fail" having run nothing; `node --test "dist/**/*.test.js"` → "2 tests, 1 pass, 1 fail", correct. It exits 1, so it fails loudly — but with an error that looks nothing like a test failure. | **The gate is `tsc && node --test "dist/**/*.test.js"`.** Quote the glob so Node expands it rather than the shell. `cd dist && node --test` also works. **Lesson: a fix to the verification mechanism must itself be verified by a probe containing a deliberately failing test — otherwise you have only proven it does not crash.** | all |
| T56 | **execa merges a supplied `env` OVER `process.env` unless you pass `extendEnv: false`.** | The child-env **allowlist withholds nothing** — both secrets reach the spawned agent — and D-15-amended plus T28 are defeated silently. Worse, a test asserting on `buildChildEnv()`'s *return value* still passes; only a test asserting on what the **spawn actually received** catches it. | `extendEnv: false` at every execa spawn that passes `env`. Verified present at `supervisor.ts:76`. Note `node:child_process.spawn` has the opposite default (it replaces), so the bug is execa-specific. Any phase adding an execa spawn inherits this. | 4, all |
| T57 | **A resumed session must still carry `-p <answer>`.** Plan and research both said "replace `-p <prompt>` with `--resume`". | Taken literally, the worker resumes the session and **says nothing to it — the human's answer never arrives.** The entire Q&A feature is inert while every log line looks healthy. | `buildResumeArgs` emits both `-p` and `--resume`, and drops `--session-id` (that is the flag that hard-errors on reuse, verified exit 1). | 4, 6 |
| T58 | **`AgentResult` is contested between two merged phases.** Phase 6's `run-engine.ts` assumes `status: 'complete' \| 'needs_input' \| 'cancelled'` with `prTitle`/`prBody`/`assumptionIfUnanswered`; Phase 4's live-verified schema is `status: 'delivered' \| 'needs_input'` with `question`/`assumption`/`summary`. | The schema sets **`additionalProperties: false`**, so if Phase 6's fields survive reconciliation they must be added explicitly or **the agent physically cannot return them**. | Phase 7 picks one shape. The schema lives in `agent-args.ts`; 04-01 deliberately did not create `src/domain/agent-result.ts`, because doing so would have hard-coded the losing side. | 4, 6, 7 |
| T59 | **Two competing child-env constructions for the same spawned process.** `run-engine.ts:511` builds `env: {LAW_RUN_ID, LAW_ISSUE_KEY, LAW_REPO}`; `execute-run.ts:178` builds `env: buildChildEnv(runId)` (the security allowlist). | Whichever reaches `supervisor.ts` wins and the other is lost — either the agent loses `PATH`/`HOME` and cannot find its own config, or the `LAW_*` correlation vars vanish from the child. | Phase 7 merges them at the seam: `{...buildChildEnv(runId), ...lawVars}`, with the allowlist as the base so security is not overridable by correlation metadata. | 4, 6, 7 |
| T60 | **T13 was applied too broadly and produced worse code.** `gh pr create` genuinely has no `--json` — but **`gh pr list --json` and `gh pr view --json` both exist** (verified on gh 2.98.0). A grep gate banning `--json` across the whole delivery file forced `findOpenPrUrl` into parsing `gh pr list` text as "first integer of the first non-empty line". | An unverified, fragile text parse of non-TTY output, chosen *because* of a trap — when the structured, supported call was available the whole time. The parse could not even be tested, since rush mode forbids running `gh`. | **Scope the ban to `gh pr create` only.** Rewrite `findOpenPrUrl` as `gh pr list --json number,url`. Phase 7 owns this. General lesson: a negative grep gate must name the *subcommand*, not just the flag. | 4, 7 |
| T61 | **`timedOut` is never threaded to the verdict.** `runAgent` returns it and `classifyOutcome` accepts it, but `execute-run.ts` does not pass it. | A run killed at its timeout **with commits present classifies as `delivered` rather than `partial`** — so a truncated run ships a PR described as complete. Silent, and exactly the misclassification the evidence-verdict design exists to prevent. | One-line thread at the `execute-run.ts` call site. Phase 7. | 4, 7 |
| T62 | **Linear-supplied `branchName` reaches a filesystem path.** `issue.branchName` is server-provided text that becomes a git worktree directory name. | Path traversal into the operator's filesystem via a crafted issue title, on a machine holding their real credentials. Not in any plan or threat register — 04-02 added the guard unprompted. | `isUnderRoot` containment check (resolved path + trailing separator) **before any subprocess runs**, plus bounded collision suffixing that never emits `-B`. Already implemented; keep it. | 4 |
| T63 | **The untrusted-ticket-data delimiter was escapable.** `buildAgentPrompt` wrapped the Linear issue body in an opening/closing tag but never defanged a **closing tag appearing inside the body**. | A ticket containing `</untrusted-ticket-data>` breaks out of the wrapper and everything after it reads as first-class prompt instructions — D-14's second layer defeated entirely, on a process running with the operator's real shell. | Defang the closing delimiter (rewrite to a bracket form). **Order matters: strip control/zero-width characters FIRST, then rewrite** — stripping last can reassemble a tag that was split by an invisible character. Fixed in 04-03. | 4 |
| T64 | **The agent physically cannot return its assumption.** `AgentResultSchema` (`src/domain/agent-result.ts`) names the field `assumptionIfUnanswered`; `src/execution/verdict.ts:79` reads `structured.assumption`. The schema sets `additionalProperties: false`. | Every `needs_input` result carries `assumption: undefined` — so when a question times out, **the worker posts no assumption to the ticket**, which is the entire fallback QA-05 rests on. Silent: the run continues and the logs look healthy. | Rename the domain field to `assumption` — it is what CLI 2.1.259 was observed returning, and `verdict.ts`/`prompt.ts` already use that name. Phase 7, one line. Related to T58's schema contest. | 4, 6, 7 |
| T65 | **19 of the 68 integration errors are the domain being RIGHT, not wrong.** `TicketRun.state` is `null` by design (Phase 1 D-04) — the parent run's status is *derived* from its children and never stored, so parent and children structurally cannot disagree. The call sites had not caught up. | The instinctive fix — flatten the union, or give `TicketRun` a state column — turns **19 red lines green in one edit** and silently deletes the guarantee that stops one repo's failure discarding another repo's already-shipped PR (DELV-07). It would look like excellent progress. | Fix at the call sites, never at the type. **An error count is not a defect count**: a type that is correct and inconvenient produces errors, and driving the number down is not the same as being right. | 6, 7 |
| T66 | **One error can hide four.** `config.ts:69`'s `as Config` cast compressed three separate toggle renames (`slackNotify`, `questionFlowEnabled`, `maxRunTimeMs`) into a single diagnostic. | Fixing the one visible error reveals three more, so "68 errors" understates the work — and a cast left in place means the *next* drift is silent. | Drop the cast rather than satisfying it. A cast at a non-boundary is a diagnostic suppressor, not a type. | 2, 7 |
| T67 | **Adding an eighth `DomainEvent` kind without settling the mapping widens the silent hole.** `questions.ts` emits `run.resumed`, which the union does not carry (inventory item M7). | T45's union already typechecks clean while the ingress→engine mapping is unwired — that *is* the hole. Adding a kind the mapping does not handle makes it bigger while looking like progress. | **M7 and hard deliverable #1 are one decision, not two.** Settle the mapping and the eighth kind in the same change. | 7 |
| T68 | **better-sqlite3 refuses to bind a JavaScript boolean.** `updateRun(id, {cancelRequested: true})` was a driver-level `TypeError`. | Invisible to `tsc` — the type is `boolean` and the column is `INTEGER`, and only the driver objects, at runtime. Cancellation would have crashed on first use. | Map booleans to 0/1 at the store boundary (`domain-store.ts`). Applies to every boolean column: `cancel_requested`, `resumable`. | 2, 7 |
| T69 | **`findActiveRunByIssue` had `LIMIT 1` while the port returns an array.** | **A DELV-07 violation, not a style issue:** unassigning a ticket fanned across three repos would cancel only the newest child and leave the other two running. Both signatures typecheck — one returns `Run[]` of length ≤1. | `.all()`, not `.get()`. Found only by exercising the real store against the port; the two `Store` interfaces had never met. | 2, 6, 7 |
| T70 | **`findQuestionByShortCode` was declared on the port, implemented in the fake, and missing from the real store.** | Tier-2 answer correlation was a `TypeError` waiting — the same shape as T46/T53. A fake that implements more than the real thing hides the gap it exists to expose. | Implement it. **Third instance this milestone of "the fake is more complete than the store"** — that pattern deserves its own check at every seam. | 2, 6, 7 |
| T71 | **An instrument that has never failed is not known to work.** 07-03 falsified both new smoke checks before trusting them: a dead port produced "nothing is listening on 127.0.0.1:56417", and an unwired mapping reproduced T45 exactly ("timed out waiting for a queued run"). Both reverted. | This is T55's lesson generalised — the verify command itself was once broken while looking fine. A green check that has never been shown to go red proves nothing. | Every new gate, probe or smoke test gets deliberately broken once, before it is relied on. | all |
| T72 | **Phase 5 and Phase 6 both built Linear comment posting.** `LinearCommentChannel` (Phase 5) fires on every event kind, while the run engine (Phase 6) already posts every Linear comment — with edit-in-place queue positions, threaded question ids and multi-repo rollups the channel does not have. | Wiring both **double-posts every milestone** to the ticket, and each bot comment is an event the four loop guards must then filter. 07-04 wired the log channel and deliberately left the Linear channel out. | Accepted for v1: the notifier is two channels, not three. Consolidating the engine's four comment sites onto the channel is a Phase 5/6 redesign, not an integration fix. **The log channel — the half the engine does not do — is wired and structurally unremovable.** | 5, 6 |
| T73 (**FIXED** 2026-09-06) | **`partial` is unreachable from a live run, so the evidence-based verdict is not the one in the live path.** Two verdict designs exist: the run engine trusts the agent's self-reported `status: 'complete'`, while `execute-run.ts` judges by commits present in the worktree. **Only the engine's is wired.** | Research Pitfall 3 is emphatic that judging by exit code or self-report is the project's most likely silent failure — `claude -p` exits 0 having been denied every edit. The wired path trusts the agent, and a truncated run cannot reach `partial` to ship its draft PR. | **Fixed.** `partial` added to the domain `AgentResult` (deliberately NOT to the wire schema — the agent must not self-report it); the adapter now gathers worktree evidence and runs Phase 4's `classifyOutcome` instead of trusting `structured_output.status`; the engine gained a `partial` arm that ships a **forced draft** with a warning banner; `Deliverer.deliver` gained a `draft` override. **The first attempt was itself incomplete** — `timedOut` short-circuited to `failed` ahead of the classifier, leaving `partial` unreachable exactly as before, and only a probe caught it. Four tests in `src/cli/adapters.verdict.test.ts` pin the outcomes apart. T61's `timedOut` threading IS closed, and the adapter routes a timeout to a named `failed`. **Highest-value follow-up after this milestone.** | 4, 6 |
| T74 | **`as unknown as` in a test fixture hides exactly the drift the compiler exists to catch.** `scheduler.test.ts:27` builds runs as `({id, state}) as unknown as Run` — with no `kind` field — so `syncFromStore`'s `run.kind === 'repo'` check skips every one and 7 tests fail against a **correct** implementation. `configWith` likewise still nests `concurrency` under `defaults`, which 07-02 moved to `Config` top level. | A fixture built as a real `Run` would have been a compile error the instant `kind` became required. The cast bought convenience and paid in silent drift — and the failures then look like implementation bugs, which is the expensive part. **45 of the 56 `as unknown as` in this repo are in test files.** | Build fixtures as real domain objects, or through a typed factory. Reserve `as unknown as` for genuine trust boundaries — wire payloads, SQLite rows, stream events — where a cast is the honest answer. | all |
| T75 | **OPS-02's WAL requirement has never been verifiable as written.** `sqlite-store.test.ts` asserts `journal_mode === 'wal'` against a `:memory:` database — SQLite cannot use WAL without a file, so it reports `'memory'` and always will. | The test is not merely failing; it **cannot pass**, so the requirement it exists to prove has never been checked at all. A file-backed fixture is required. | Open a temp-file database for that one assertion and clean it up. Note the same limitation applies to any future durability or concurrency assertion — `:memory:` is not a substitute for a real file when the property under test is a file-level one. | 2, 7 |
| T76 | **A grep gate matching prose is vacuous.** 07-05 falsified its own plan's gates: `SIGTERM` appears in a *comment* in `daemon.ts`, so **deleting the signal handler still passed the gate**. The process-group gate likewise matched `-pid` inside a comment. | The gate reports green over the exact deletion it exists to prevent — worse than no gate, because it is trusted. Same family as T60, where an over-broad `--json` ban produced worse code. | Assert behaviour, not text: `process.listenerCount('SIGTERM')` in the smoke, and a named-grandchild assertion for the process-group reap. **Every grep gate in this repo should be falsified by deleting what it guards and confirming it goes red.** | all |
| T77 | **The webhook signing secret was never registered with the logger.** It is generated at runtime (Phase 3) and `registerSecret` existed and worked — verified by execution — but nothing called it for that secret. | The signing secret would have been written to logs in plaintext. The redaction mechanism being correct is not the same as it being *wired*, and a probe of the mechanism passes either way. | Call `logger.registerSecret` at the point the secret is minted. Fixed in 07-05. **Verifying a capability is not verifying its call site.** | 3, 7 |
| T78 | **`run.requested` had no live-run guard: one ticket could produce two runs, two worktrees and two PRs.** The reconciliation poll and a retried webhook delivery are two independent producers of the same event, and neither checked for an existing live run. | Duplicate PRs for one ticket, duplicate Claude sessions against the concurrency cap, and the operator seeing the bot argue with itself. The delivery-ID guard does not help — these are two *different* deliveries describing the same issue. | Guard in the engine, where both producers meet — not in either producer. Fixed in 07-05. | 3, 6, 7 |
<<<<<<< HEAD
| T79 | **`package.json` has no `bin` entry, so `law` is not installable as a command.** Every plan, doc and UAT step says "run `law setup`"; nothing makes that binary exist. | SETUP-01 and the operator's explicit "one-line install" requirement are unmet at the packaging layer, while the wizard code behind them is complete. No phase owned packaging — it fell between Phase 8 (wizard behaviour) and Phase 7 (integration). | Add `"bin": {"law": "dist/src/cli/index.js"}` plus a shebang, and document `npm link` (or `npm i -g .`) in the README. Verify by actually running `law` from a different directory. | 7, 8 |
| T80 | **`law --help` exits with a raw `ERR_PARSE_ARGS_UNKNOWN_OPTION` stack trace.** `node:util.parseArgs` throws on any unknown *option*; the unknown *command* path is handled and prints `usage: law <setup [--doctor]\|start\|status>`. | `--help` is the first thing an operator types. 08-CONTEXT D-08 requires an actionable message and never a stack trace — the requirement is met for commands and missed for options. | Wrap `parseArgs` in try/catch and print the same usage line; add `--help`/`-h` explicitly. | 8 |
| T81 | **No README.** The only operator-facing doc in the repo is `docs/agent-invocation.md` (04-03's trust-risk statement). | Same unowned-work shape as T79: `REQUIREMENTS.md` scopes 70 *behaviours* and nothing scopes documentation, so no phase produced any. A tool whose entire premise is "assign a Linear issue, get a PR" has no file saying how to start it. Matters most if this is published — the traps ledger is the genuinely novel asset and it is currently buried in `.planning/`. | Out of the milestone's stated scope, so not written. Decide deliberately: a README covering install, the two secrets, the mapping file and `law setup`, and — if publishing — the traps ledger as first-class content rather than an appendix. | — |
=======
| T87 | **A fake command runner that answers exit 0 to everything answers "YES" to every probe.** `git show-ref --verify --quiet refs/heads/<b>` exits 0 to mean *the branch already exists*. Three separate files shipped a blanket-success double — `07-lifecycle.test.ts`'s `okTools`, `execute-run.test.ts`'s `ok('')` fallthrough, and by extension anything using `BootOptions.runCommand` as if it were preflight-only. | `resolveBranchName` burns all 50 suffixes and `prepareWorktree` throws `could not find a free branch name` — **50 branch names away from the fixture that caused it**, and in the lifecycle case it presented as three tests timing out waiting for an agent that was never spawned. | A command double must distinguish **probes** from **commands**. Cheapest correct answer: let `git` run for real against a scratch repository (it is local, and `makeScratchRepo` already exists) and fake only `gh`/`claude`. `BootOptions.runCommand` is NOT preflight-only — it also reaches the worktree manager and the deliverer, as its own doc comment says. | 4, 7 |
| T88 | **`mock.method` on an ESM namespace object cannot work, and never could.** An ESM binding is non-configurable *by specification*; `mock.method(execaModule, 'execa', …)` throws `Cannot redefine property: execa`. 22 of the milestone's 56 first-run failures were this one line, across three wizard suites, against two different modules (`execa` and `@inquirer/prompts`). | The failures look like 22 independent bugs in the wizard. They are one testing approach that the module system forbids. | **Default-parameter injection**, which 03-01 already uses for the ngrok SDK (`openTunnel(port, ngrok: NgrokApi = ngrokSdk)`) — production call sites stay unchanged. Reuse the existing `RunCommand` port for child processes rather than inventing a second one. Do **not** reach for `mock.module`: it is `undefined` on Node 22 without `--experimental-test-module-mocks`, and that flag would live in the canonical gate forever. | 8, 7 |
| T89 | **`tsc` emits `.ts` and nothing else.** `src/execution/fixtures/stream-events.jsonl` never reached `dist/`, so both suites that replay the captured agent stream failed with ENOENT — the gate runs the compiled output. | Four failures that look like fixture-path bugs in two unrelated files. The tests resolve `./fixtures/` relative to their own location, which is correct; the *build* was incomplete. | An `assets` step in `build`/`test`/`verify` (`fs.cpSync('src','dist/src',{recursive:true,filter:f=>!f.endsWith('.ts')})`) so the next non-`.ts` fixture is covered without anyone remembering. | 7, all |
| T82 | **A liveness check on the positive pid cannot see the process the escalation exists for.** `escalate()` asked `isAlive(pid)` between steps — the group LEADER. POSIX *requires* a non-interactive shell to set SIGINT/SIGQUIT to SIG_IGN for a background job, so the leader dies to SIGINT, the leader-only check reports "gone", the ladder stops, and the grandchild survives. | Precisely the T29/D-09 orphan the escalation was written to prevent, reached through the check that was supposed to prevent it. Every other assertion in the reap test passed. | Negate the pid for liveness exactly as for the signal: `process.kill(-pgid, 0)` asks *is anything in the GROUP alive*. Document the convention on the `IsAlive` type so an injected double cannot quietly disagree. | 4, 7 |
| T83 | **Module-level state leaks between test cases in the same file.** `noteSelfWrite`'s suppression map is module-scoped with a 90-second window. Three earlier cases in `linear-client.test.ts` called `setIssueState('issue-1', …)`, so T49's own falsification line — "with no self-write recorded the guard must PASS" — read `true` against a correctly wired guard. | Diagnosed at the milestone level as "`noteSelfWrite` is not registered at the layer the test checks". It **is** registered, at `linear-client.ts:295`. An hour of looking for a wiring gap that did not exist. | Any case asserting on a process-global side-effect table must use an identifier no other case in the file touches. A `resetForTest()` export would be worse — production API bought for tests. | 5, 7 |
| T84 | **An `async` function cannot return a memoised promise by identity.** `daemon.ts`'s `shutdown()` memoised its work correctly, but `async` wraps every return in a *fresh* promise, so `shutdown('a') !== shutdown('b')` and the idempotence contract was unobservable from outside. | The behaviour was right and the contract untestable — the failure mode is a later refactor breaking idempotence with no assertion able to notice. | Drop `async` when the body is already an IIFE and the function's job is to hand back a stored promise. The keyword bought nothing and cost the identity. | 7 |
>>>>>>> gsd/07-06

*(appended as waves hit them)*

## Discovered at open-source release

| # | Trap | Failure mode | Correct move | Phase |
|---|------|--------------|--------------|-------|
| T90 | **A structurally-valid fake credential in a test fixture is blocked by GitHub push protection.** `gates.test.ts:74` held `xoxb-1111111111-2222222222-<24 chars>` — the correct shape, which is the point of the fixture. | The **first push of the public repo is rejected** (`GH013`), and the offending blob is 262 commits back, so fixing HEAD does not help. Contributors' local scanners flag it too. Notably `ghp_AAAA…`/`sk-ant-…` did NOT trip (vendor checksum/entropy checks) and `AKIAIOSFODNN7EXAMPLE` is allowlisted — only the purely structural Slack detector matched. | Assemble the value at runtime — `` `${'xox' + 'b'}-…-${'a'.repeat(24)}` `` — so the source holds no matching literal while the tested string is byte-identical. **Do not** use the "allow this secret" unblock link: it records a push-protection bypass on a public repo. Then falsify (measured: 21/1 broken, 22/0 restored). Rewrite the historical blob with `git filter-repo --replace-text`. | release |
| T91 | **`bootDaemon` runs a real preflight, so any test that boots it without `runCommand` silently depends on the developer's `gh` login.** Two of the three integration suites omitted it. | Passes locally, fails on **any** machine without an authenticated `gh` — the first CI run of the public repo failed **14 tests** on all four matrix legs while the same commit was green locally. The failure text (`law start: gh is not usable`) reads like a product bug, not a test-harness bug. | Every `bootDaemon` in a test passes `okTools` (now exported from `src/cli/daemon-fixture.ts`, not redefined per suite). Reproduce by putting a `gh` shim that exits 1 first on `PATH`: measured 0/14 with it, 14/14 without; after the fix, 511/511 with it. CI must stay **unauthenticated** — adding `GH_TOKEN` would make the tests pass without fixing them. | release |
| T92 | **Two migration runners, one of them dead — and the dead one had all the tests.** `migrate.ts` (asserts strictly ascending versions, explicit BEGIN/COMMIT, returns what it applied, seven tests) was never called by anything. `openStore` used a duplicate inside `db.ts` with its own hardcoded `MIGRATIONS = [migration001]`. | Adding migration 002 to `migrations/index.ts` — the list the *tested* runner reads — applies **nothing**. `tsc` passes, every test passes, the column does not exist, and the first failure is a `SELECT` on a missing column at runtime. Found by writing migration 002 straight into the trap. | One runner, one list. `openStore` calls `migrate(db)`; `db.ts`'s duplicate is deleted. Regression check asserts `openStore` leaves `user_version` at the newest migration, so a future third migration cannot be silently dropped. | release |
| T93 | **A substring secret-key redactor eats innocent fields.** `SECRET_KEY_PATTERN = /(token\|secret\|key\|authorization)/i` matched `tokensUsed` — a token *count* — so every terminal log line printed `"tokensUsed":"[REDACTED]"`. | Invisible for the whole milestone because D6 had the value hardcoded to `0`; it surfaced the moment the field carried a real number. The tempting fix (anchor or narrow the pattern) trades a cosmetic bug for a credential leak. | Keep the broad pattern — over-redaction is the safe error for a log sink — and add an explicit `NOT_SECRET_KEYS` exception list, so anything new still fails closed. A second test asserts secret-named fields still redact, and it goes red if someone loosens the pattern instead. | release |
| T94 | **A test fixture's stub store silently constrains production code.** `adapters.verdict.test.ts`'s store had only `getRun`; adding a `store.updateRun` call to the runner threw `is not a function` in every case. | Reads as the new code being broken rather than the fixture being thin — and the temptation is to move the production write somewhere less correct to keep the stub happy. | Widen the fixture, never the production code, and say in the fixture WHICH test owns asserting the new write (here, `usage.test.ts`, against a real SQLite file) so the same behaviour is not pinned twice. | release |
| T95 | **A per-session value assigned to a per-run row under-reports exactly the runs that matter.** Gap D6's first implementation did `updateRun(runId, { costUsd: resultEvent.total_cost_usd })`. One run is one row but can be several `claude` sessions — every answered question resumes the run through the same function. | The recorded cost is the LAST session's, not the run's. A run that asked three questions reports a quarter of what it spent, and the under-reporting scales with the number of sessions — so the most expensive runs are reported the most wrongly. Nothing fails; the number is just quietly false. | Accumulate: read the row, add, write. Caught by asking "can this code path run twice for one row?" of every write, not by any test that existed. The regression test runs the same runner twice and asserts the sum. | release |
| T96 | **A fourth dead duplicate: `executeRun`.** Phase 4's complete composition root for one run — worktree, prompt, supervised spawn, verdict, deliver — 272 lines with a 281-line test file, called by nothing outside its own test since Phase 6 built the real path. | The same shape that produced T72 (two comment posters), T73 (two verdict designs, the evidence-based one dead) and T92 (two migration runners, tests on the dead one). It was about to produce a fourth: wiring `--max-turns` and `--max-budget-usd` needed doing in two places with nothing to catch a drift. | Deleted. Kept only what other modules import (`RunCommand`, `defaultRunCommand`, `ExecutionVerdict`). **Find these by grepping for CALLERS of every exported entry point, not for definitions** — all four were invisible to `tsc`, to the test suite, and to code review. | release |
| T97 | **`--max-turns` is a real, validated `claude` flag that does not appear in `--help`.** Absent from the flag list on 2.1.259, so it reads as removed. | A project that greps `--help` to decide what is supported concludes the turn cap cannot be set, and ships a `maxTurns` config key that does nothing (which is exactly what happened here). | Probe the flag with an invalid value instead of trusting `--help`: `--max-turns notanumber` answers `option '--max-turns <turns>' argument 'notanumber' is invalid. must be a number`, whereas an unknown flag answers `unknown option '--x'`. The two messages are the test. | release |
| T98 | **`--max-budget-usd` rejects zero and negatives** (`must be a positive number greater than 0`), so a *remaining*-budget calculation cannot simply pass what is left. | An exhausted run either crashes at spawn or, if the caller clamps to 0, gets an unbounded session — the opposite of the cap's purpose. | Decide before the spawn: if nothing remains, do not start a session, and route the outcome through the same evidence-based verdict the deadline reap uses so committed work still ships as a `partial` draft rather than being discarded for running out of money. | release |
| T99 | **The prompt-injection containment was never on the live path — and neither was the brief.** `run-engine.ts` spawned the agent with `prepared.issueTitle`: the raw ticket title, alone, as the entire prompt. `buildAgentPrompt` — which composes the task, states the delivery contract, and wraps Linear-authored text in a defanged `<untrusted-ticket-data>` delimiter — was dead code, under a comment in the run engine saying the real brief "is composed in execution/prompt.ts, Phase 4 owns it". The resume path passed the human's Linear COMMENT to `-p` verbatim. | Three defects at once, all silent. (1) The agent implemented a one-line title with no description, URL, branch or instruction to run GSD. (2) It was never told "commit, do NOT push" — the contract that keeps every push behind `gates.ts`'s secret scan and default-branch refusal. (3) There was no instruction/data boundary on ticket text at all. `07-RUNTIME-EVIDENCE.md` records two injection attacks as **PASS**; they were exercising a builder no run ever called. SECURITY.md described containment the product did not have. | Call it, and **assert the prompt's CONTENT end-to-end**, not just the builder's output in a unit test. The whole suite — 544 tests — passed before and after the fix, because nothing had ever asserted what reached `-p`. For any security control: find the test that fails when the control is removed *from the live path*. If the only such test targets the control's own module, the control is not known to be wired. | release |
| T100 | **A multi-repo ticket's agents did not know about each other.** `fanout.ts` exported `ticketBriefRepos(plan)` under a comment saying "composing the brief is Phase 4's prompt work; this plan supplies the list". The prompt builder never took a repo list. Its test was titled *"so a child knows it is one of several"*. | A ticket mapped to three repos produced three agents each implementing as if theirs were the only repo — with a green test whose name asserted the opposite. The helper's only caller was that test. | The list is in the brief now, sourced from `store.childRuns(parentRunId)` rather than from the plan: the plan exists at fanout time, the brief is composed later per child. The prompt also says explicitly that the siblings are unreachable — an agent told a sibling repo exists and NOT told that will try to coordinate with it. | release |
| T101 | **Two `holdsSlot` functions reading two different sources.** `domain/state-machine.ts` returned `HOLDS_SLOT.includes(s)`; `orchestration/scheduler.ts` defined its own returning `RUN_STATE_TABLE[state].holdsSlot`. Same three facts (`holdsSlot`, `hasLiveChild`, `terminal`) expressed twice, agreeing only by luck. | A divergence is not cosmetic: the scheduler admits work by one and the daemon decides what is "in flight" at shutdown by the other, so disagreement means a run holding a slot nobody accounts for, or a run requeued while its child is alive. Measured at the time: they happened to agree for all nine states. | One function (the scheduler re-exports the domain one) plus a test asserting the table and the arrays agree for every state. Where two representations of the same fact must both exist, the cheap fix is not to unify them but to assert their equivalence. | release |
| T102 | **`installTunnelShutdownHooks` was exported, uncalled, and calling it would have caused a bug.** It registered SIGINT/SIGTERM handlers that ran `ngrok.kill()` then `process.exit(0)`. Its comment said "Phase 7 owns daemon lifecycle and calls this once, explicitly". Phase 7 never did. | Installing it would have raced the daemon's ordered shutdown and `process.exit`ed mid-way, abandoning the in-flight run marking — the single write that stops a Ctrl-C from costing a manual re-assignment of every live ticket (T18/T26). Its own test had to detach the handlers in a `finally` to avoid killing the test runner, which was the clue nobody read. | Deleted, with the reasoning left in the module so it is not re-added. **A dead export is not automatically a missing wire** — check whether calling it would be correct before wiring it. Two of the seven found in this sweep were like this. | release |
| T103 | **Linear "Agents" (app users) are the obvious bot answer and are incompatible with assignee-based pickup.** Linear's docs: agents "can be mentioned, **delegated issues through assignment**", and "are not counted as billable seats" — which reads like exactly the free bot account this design wants. | Two lines further down: "Agents are **not traditional assignees**. Assigning an issue to an agent **delegates** the issue to that agent while the human teammate **remains the primary assignee**." This daemon routes on `assigneeId`, which stays the HUMAN — so the daemon never fires. And "agents cannot access admin functionality", so an agent cannot create the webhook either. | For today: the bot is a **second human-shaped Linear account with workspace admin**, and its personal API key is what the daemon uses (there is no such thing as a non-personal Linear API key). Supporting Agents properly means routing on the delegate field as well as `assigneeId`, plus an OAuth app for identity — a feature, not a setting. Recorded so nobody spends a day trying to configure it. | release |
| T104 | **Suspending the bot user silently revokes its API key.** Linear's docs: "What happens to API tokens when a user is suspended or converted to a guest? **The API tokens will be revoked and invalidated.**" Suspending an idle bot is the obvious way to stop paying for its seat, since billing counts *unsuspended* users. | The daemon starts failing 401 on every Linear call with nothing in its own logs naming the cause — the key was valid yesterday and nothing about the daemon changed. Converting the bot to a Guest to save money fails the same way, and guests are Business/Enterprise-only and billed as regular members anyway. | Do not suspend the bot account. On a **Free** workspace the seat is free and every member is an Admin automatically, which is also what `webhookCreate` needs — so Free is the cheapest correct home for a bot. On a paid plan, budget one seat. If a 401 storm ever appears from nowhere, check the bot's member status before anything else. | release |
| T105 | **`tsc` emits 0644, so a `bin` entry is not executable — and `clean` destroys the bit npm sets.** `package.json` has `"bin": {"law": "dist/src/cli/index.js"}` and the file carries `#!/usr/bin/env node`, but TypeScript does not preserve or set an executable bit, and `build`/`verify` both begin with `clean` deleting `dist/`. | `npm link` chmods the bin target, so install works **once**; the very next `npm run build` or `npm run verify` silently reverts it and `law` dies with `zsh: permission denied: law` (exit **126**). Reported by the operator on the first real use, after the whole packaging pass had been called done — `--help` had been tested by running `node dist/...` directly, which needs no exec bit. | `npm run assets` (already on every build path) now `chmodSync(..., 0o755)`. Regression test asserts `mode & 0o111 === 0o111` on the BUILT artifact, not on the npm script — the claim is "the thing on PATH runs", not "the script exists". Falsified at 644: test red, `law` exit 126; after `assets`: green, exit 0. **Test a CLI by invoking it the way the operator will, not by running its entry file with `node`.** | release |
| T106 | **A test fixture sized 2 hides a UI defect that only exists at 277.** `promptRepoSelection` rendered every discovered repo in one `checkbox`. Seven tests covered it, all green, all using a two-repo fixture. Against a real root (`~/ohmaseclaro`) `discoverRepos` returns **277**, and inquirer's checkbox paginates at 7 — 40 pages with no search. | The step was correct, tested, and unusable. No assertion could have caught it, because every assertion was about *behaviour* and the defect was about *scale*. Reported by the operator on their first real `law setup`, mid-flow. | Run your own UI against production-sized input before calling it done — here, one `discoverRepos(~/…)` call would have shown 277 at any point in the previous eight phases. The fix composes `input` (filter) → `checkbox` (matches only) → repeat, because `@inquirer/prompts@8.7.1` has no searchable multi-select (verified by importing it: `Separator, checkbox, confirm, editor, expand, input, number, password, rawlist, search, select`). Small sets (≤20) keep the single-prompt path so the common case does not regress. | release |
| T107 | **The reconciliation poll had no self-event guard, so the bot's own writes re-triggered its own delivered ticket once a minute.** `recovery.ts` gated the poll on `findActiveRunByIssue`, which filters terminal states out — a `delivered` run blocks nothing, and the issue is still assigned and still open, so it re-qualifies on every tick. The watermark cannot bound it either: the bot's OWN In Progress transition and "Done" comment bump `issue.updatedAt`. This is the self-event problem on the POLLING path — the webhook path has loop guard L1 (delivery `actor` vs `botUserId`), but a poll observes a *state*, not an *act*, so it has no actor to compare. | A new worktree, a new pull request and a new paid `claude` session (~$0.51 / 193k tokens) **every minute, forever**. Observed live on COD-2 sixty seconds after its PR was opened; the daemon could not be run at all. Latent for two phases because `reconcile` used to run only at boot — gap D4 added the periodic tick and turned a restart-only annoyance into a loop. Same defect as T78 (one ticket, two runs) through the other door, and T78's fix carried a comment stating the wrong theory: *"the guard against an automatic retry loop is the reconciliation watermark, not this check"*. | Put the **evidence class on the event** — `run.requested` gains a REQUIRED `trigger: 'assignment' \| 'reconcile'` — and put the rule in the **engine**, the one place both producers meet. An `assignment` is an act with an actor deduped by `Linear-Delivery`, so it is blocked only by a LIVE run and the operator's unassign→reassign retry still works; a `reconcile` is an observed state, so it is blocked by ANY run, terminal included. The poll's private copy of the check was **deleted, not extended** (net: one guard site, down from two — T72/T73/T92/T96/T99/T101 are all two-implementations-of-one-rule), and its `report.enqueued` now observes a run row appearing instead of re-deriving the rule. Required-not-optional is the point: a permissive default silently restores the loop, a strict one silently breaks reassignment, and silent-unwired is this repo's signature failure. Measured: the end-to-end poll test red at `2 !== 1` before the fix, 590/590 after; `findActiveRunByIssue` as the predicate is red the same way; firing on `assignment` too is red on the deliberate-re-request case at `1 !== 2`. | 3 / 6 / 7 |
| T108 | **A state written to be picked up later, with nothing that picks it up.** Two writes exist solely for a later read: `markInFlight` (clean Ctrl-C, `running` -> `queued`) and `recoverAtBoot` (`preparing` -> `queued`). There was no later read. `scheduler.pump()` drains an **in-memory** `waiting` array populated only by `acquire()` calls THIS process made, so a `queued` row in SQLite is not in it, and nothing at boot read the queue from the store. `Store.nextQueued` — `SELECT * FROM runs WHERE state = 'queued' ORDER BY created_at ASC LIMIT ?`, the exact query needed — was defined at all four seams, tested in `sqlite-store.test.ts`, and called by nothing. Seventh dead export of this shape after T72/T73/T92/T96/T99/T102, with `RepoRun.resumable` an eighth and write-only-always-zero `attempt` a ninth. `recovery.ts` carried the lie in one line: `// Already correct. It will be picked up by the normal queue drain.` | Every Ctrl-C silently cost a manual re-assignment of every live ticket — the exact cost `daemon.ts`'s own comment says `markInFlight` exists to prevent (T18, T26). Measured: a clean Ctrl-C requeued an in-flight run, the next boot logged `boot recovery complete {left: 1}`, `law status` showed the run ACTIVE, capacity 3 / in use 0, and four one-minute tick cycles passed with zero agent spawns. It sits there forever, reported as running. | `engine.dispatchQueued()`, wired at `daemon.ts` step 8b AFTER `scheduler.start()` (before it, the drain runs while the scheduler is not admitting and silently does nothing — this bug's own failure mode). Proved by a **SECOND BOOT** in the boot smoke, not by a unit test: with the `daemon.ts` call line deleted the unit suite stays 590/590 GREEN and only the smoke goes red (T99 re-measured). **Two findings a unit test could not have produced.** (1) `--session-id` HARD-ERRORS on a spent id (`Session ID <uuid> is already in use`), and a run requeued out of `running` has spent its id — so the naive `drive()` is green against `FakeAgentRunner` and **0% correct in production**; the drain mints a fresh `sessionId` unconditionally. (2) `scheduler.start()` resolves parked drivers' slot promises as **microtasks**, so a drain in the very next synchronous statement reads those rows still at `queued` and double-drives every run the boot sweep just enqueued — two worktrees, two `claude` sessions, two PRs for one ticket; fixed by a `driving` Set added in `drive()` BEFORE `await slot` (not `aborts`, which `stop()` depends on holding only slot-holders). Also measured, against the plan: `resumable` was dead by **disuse, not structurally** — `domain-store.ts`'s `toRow`/`toRun` are generic copiers behind `as unknown as` casts, so a `resumable` write round-trips to the column and reads back as `1`, and re-adding the field type-checks clean. | **Grep for CALLERS of the write, not just for the write.** "Requeued for the next boot" is a claim about code that must exist SOMEWHERE ELSE, and this repo has now shipped that claim wrong three times. A state machine's writers are easy to find and its readers are the thing that makes them mean anything. | 6 / 7 |
| T107 | **A poll that re-derives work from state re-runs everything the bot already finished, forever.** `reconcile()` enqueued any assigned open issue with no ACTIVE run. A `delivered` run is terminal, so it did not block; and the watermark could not help because the bot's own writes — the In Progress transition and the "Done" comment — bump `issue.updatedAt` past it. This is the self-event problem on the polling path, where there is no `actor` to compare against. | Found on the FIRST live end-to-end run: COD-2 delivered PR #1, and 60 seconds later a second run was working the same ticket on branch `…-2`. Left running it opens a pull request a minute, at ~$0.50 of agent time each, forever. Latent until a periodic tick was added the same day — before that, `reconcile` ran only at boot, so it took a restart to bite. | The guard keys on the PRODUCER'S EVIDENCE CLASS, not on run state: a webhook is an act with an actor (blocked only by a LIVE run — an unassign/reassign must still work); a poll is a state the bot's own writes keep refreshing (blocked by ANY run, terminal included). One guard, in the engine, on a required `trigger` field of `run.requested` — and the poll's private copy was DELETED, so the net is one guard site, not two. **If a code path derives work from state rather than from an event, ask what the bot's own writes do to that state.** | live UAT |
| T108 | **`markInFlight` and `recoverAtBoot` both wrote `queued` for a next process that never read it.** `scheduler.pump()` drains an in-memory array populated only by `acquire()` calls this process made; nothing read the queue from the store at boot. `Store.nextQueued(limit)` — the exact query needed — was dead code in all four seams. Seventh dead export of this shape here (T72/T73/T92/T96/T99/T102). `RepoRun.resumable` was the eighth. | Every Ctrl-C silently abandoned its work while `law status` reported the run as active. `daemon.ts`'s own comment claimed this write is what stops "every Ctrl-C costing a manual re-assignment of every live ticket (T18/T26)" — it cost one anyway, invisibly. Measured live: a requeued run sat undispatched across four tick cycles with 3 free slots. | Drain at boot, AFTER `scheduler.start()` — a drain that runs while the scheduler is not admitting silently does nothing, which is this bug's own failure mode. Two traps inside the fix: a requeued run's `sessionId` is SPENT (`--session-id` reuse is a hard CLI error) so the drain must mint a fresh one — and no fake would ever catch that, because fakes never see the argv; and `scheduler.start()` resolves parked slots as a MICROTASK, so a drain on the next synchronous line re-reads those rows as `queued` and double-drives them. | live UAT |
| T109 | **A test that only fails in its own module has not proven the wiring.** The drain's smoke test was made to fail by deleting its single call line in `daemon.ts`: the 594 unit tests stayed GREEN while the smoke went RED. That difference is the proof it is on the live path. | Applied as a deliberate procedure after T99, where prompt-injection containment was unit-tested and never called. Running it here caught a vacuous first attempt: the initial assertion passed at HEAD because the shutdown case already manufactures the same event history, and only an `eventsBefore` watermark made it real. | For any control that must RUN and not merely work: delete its call site and confirm that the unit tests stay green while the integration check goes red. If both go red, the test is targeting the module; if neither does, it is targeting nothing. **Run three more times at quick 260907-voh, and one of them came back NEITHER-RED — the outcome this procedure exists to expose.** (1) `law say`: deleting `onInput:` in `adapters.ts` left `inject.test.js` 11/11 GREEN and turned `adapters.inject.test.js` RED at `no live agent for run run-inje`. (2) The run log, FIRST attempt: deleting `onEvent:` in `adapters.ts` left the ENTIRE suite 627/627 green — the module suites (`run-log.test.js`, `watch.test.js`) target the modules, so the wiring was untested and was reported as such in the commit rather than claimed. An assertion was added to the integration file and the deletion RE-RUN: module suites 19/19 GREEN, `adapters.inject.test.js` RED at `the system/init reached the log`. (3) The socket: deleting `serveInjections(...)` in `daemon.ts` left the unit suites 12/12 GREEN and `npm run smoke` `SMOKE FAILED`. The lesson from (2) is the one worth keeping — **a first pass that goes neither-red is the normal outcome, not a sign the procedure was done wrong.** | live UAT / quick 260907-voh |
| T110 | **Creating the test ticket with the bot's own API key makes the bot the actor, so the webhook is dropped by the self-event guard.** Log line: `self-event dropped {guard:"actor:self",actorType:"user"}`. Pickup silently falls back to the reconciliation sweep, up to `tickMs` (60s) later. | A live re-verification on 2026-09-07 showed a ~42s delay to `preparing` that reads exactly like a broken webhook. Anyone holding only the bot key will mis-diagnose the ingress path. Assign from the operator account to exercise the webhook itself. |
| T111 | **Worktree pruning is deferred to the next boot, not performed at delivery.** A delivered run's worktree stays on disk for the life of the daemon; boot recovery logs `pruned:[...]` and clears them. | Reads as a leak while a daemon is running, and cost a false alarm during live verification. Confirmed correct: an interrupted attempt's abandoned worktree AND local branch are pruned by the same boot pass. A long-lived daemon still accumulates them until restart. |
| T112 | **A fetch updates remote-tracking refs only, so any code that fetches and then names a bare branch has not refreshed anything.** `worktree.ts:113` fetched `origin`; `worktree.ts:135` then ran `worktree add -b <branch> <path> o.base`, where `o.base` is `RepoMapping.baseBranch` arriving from `config.json` as the bare string `"main"` (the wizard writes it). A bare name resolves to `refs/heads/main` — the LOCAL branch, which `git fetch` never moves. The fetch was inert for its own stated purpose. | Measured across the operator's three mapped clones on 2026-09-07: instantchat 0 behind `origin/main`, pomboo 0 behind, kardun **18 behind** — kardun is checked out on `production`, so its local `main` only moves on an explicit fetch of that ref. A run there branches 18 commits stale and opens a pull request against an outdated base. This is invisible on a machine whose base branches happen to be current, which is why 594 tests passed over it. Worse, the intent was stated CORRECTLY in the comment directly above the contradicting line — "Fetch before branching so a run does not fork off whatever the operator last happened to have pulled" — and that comment is exactly what made the line below it look already-solved to every reader. | Probe `refs/remotes/<remote>/<base>` with the same `show-ref --verify --quiet` + `reject:false` helper, generalised out of `branchExists` into `refExists(runCommand, repoPath, fullRef)` so there is one probe and not two (T72/T73/T92/T96/T99/T101), and check out THAT ref string rather than the short `<remote>/<base>` form so the probe and the checkout cannot disagree. Fall back to the bare name — required, not optional: a clone with no remote, and an offline run whose fetch failed, must both still work. Falsified at HEAD: unit case RED at `actual 'main'` vs `expected 'refs/remotes/origin/main'`, and it discriminates only because it scripts `show-ref` by ref NAMESPACE — every pre-existing case answers exit 1 to every `show-ref` and would have told the new probe "absent", making the obvious test vacuous. T109 wiring procedure, with `adapters.ts:96`'s `prepareWorktree` call replaced by an inline naive `worktree add`: unit 17/17 GREEN, integration RED at `actual b2d226b` (the clone's stale local main) vs `expected 3ce3766` (the upstream tip). 598/598 plus the boot smoke after the fix. | live UAT |
| T113 | **`-p "<prompt>"` is SILENTLY DISCARDED under `--input-format stream-json`, and the session then hangs forever.** Measured on CLI 2.1.263: `claude -p 'Reply APPLE' --input-format stream-json --output-format stream-json --verbose --permission-mode dontAsk --max-turns 4`, stdin held open with nothing written. Eight `system/hook_started` and eight `system/hook_response` arrived inside 0.7s and then **nothing for 90 seconds** — no `system/init`, no assistant, no result — until SIGKILL. | Every run burns its full 45-minute `maxRunMs` producing absolutely nothing, with a healthy-looking log. That is T1's exact shape reached through the very flag pair chosen to gain a stdin channel. Worse, it is INVISIBLE to `npm run verify`: the gate never spawns a real binary, so only a human-run probe can see it — and the CLI ships a new major roughly weekly, so the regression that reintroduces this is a VENDOR one nobody is watching for. | Keep `-p` (required — `--input-format` only works with `--print`), pass it **LAST and with NO VALUE**, and write the prompt as an NDJSON `user` message on stdin: `{"type":"user","message":{"role":"user","content":[{"type":"text","text":"…"}]}}`. Last because an option following a bare `-p` can be eaten as its optional value. `--replay-user-messages` is the receipt that it landed. Three containments, not one: (1) the write is synchronous, before any await; (2) `AGENT_ACK_TIMEOUT_MS` — 60s, ~85x the measured 2.6s-to-init cold start — reaps a session that produces neither a `system/init` nor any replayed `user` event, turning 45 silent minutes into one loud one **on every run, without anyone remembering to run anything**; (3) `scripts/probe-stream-input.ts` re-measures the whole contract against the real binary (~$0.30). Falsified: deleting the stdin write turns `supervisor.test.ts` RED twice — `T99/T57 relocated: the prompt reaches the child on STDIN` at `0 !== 1`, and `law say reaches a live session` at `1 !== 2`. Removing the ack reap turns the whole file RED as `failureType: 'testTimeoutFailure'`, `test timed out after 8000ms` — the run never resolves, which is M2's own shape. | quick 260907-voh |
| T114 | **Under streaming input a `result` event arrives PER USER MESSAGE, so `result` is not the terminal event.** Measured on CLI 2.1.263 (`scripts/probe-stream-input.ts`, 2026-09-07): three messages in one session produced three results, `num_turns` 3/3/3 under `--max-turns 4` — so the turn budget is per MESSAGE too. The asymmetry that rides along is the dangerous half: `total_cost_usd` is **CUMULATIVE for the session** (0.391886 → 0.434957 → 0.478395) while `usage` is **PER MESSAGE** (`input_tokens` flat at 4 while `cache_read_input_tokens` climbed 36972 → 74262 → 74807). Take the LAST cost; SUM the tokens. Getting them backwards is T95 in both directions at once. A second `system/init` is emitted per message as well (M9), carrying the same `session_id`, so `assertSessionUsable` re-runs each time — harmless, but any code assuming one init per session is wrong. | **Two different failure modes, and they must not be conflated.** (a) If OUR code treats `result` as "the run is over", it delivers a pull request after turn one while the agent is still working. (b) If **the measurement itself** stops holding — a CLI that emits a result per INTERNAL turn — the daemon is immune to (a), because `routed.result` is last-wins and nothing resolves early; what it would do instead is close stdin on an internal-turn result, EOF the child mid-work, and truncate a healthy run into a `partial`. No unit test can see (b): the suite scripts the stream, so it can only ever confirm what we already believe. The probe is the only instrument for it. | The run ends when the **PROCESS EXITS** (`Promise.race([completed, reaped])`, unchanged since Phase 4). The single thing `result` decides is when stdin closes — on the FIRST one, exactly once. That is what makes today's semantics survive: one message, one result (M4), stdin closes, EOF (M8), clean exit 0 within 0.6s. A message written before the close is still delivered, because pipe bytes precede EOF, so the operator's correction gets its own turn and its own result and `routed.result` last-wins gives it the verdict (M10). Falsified for (a): resolving on the first result turns `supervisor.test.ts` RED at `T114: the LAST result governs` with `+ 'turn one'` / `- 'turn two'`. There is no test for (b) and there cannot be one — run the probe. | quick 260907-voh |
| T115 | **`--remote-control` accepts the flag, exits 0, and does nothing under `--print`** — but the CLI opens a per-session control socket of its own regardless, and that is the more useful finding. Its help text says it "starts an INTERACTIVE session", which is exactly what `-p` is not. Measured live on CLI 2.1.263 with `--remote-control probe` passed alongside `-p --input-format stream-json --output-format stream-json --verbose`: `system/init` carried **24 keys and not one of them matches `/remote/i`** — `type, subtype, cwd, session_id, tools, mcp_servers, model, permissionMode, slash_commands, terminal_slash_commands, apiKeySource, claude_code_version, output_style, agents, skills, plugins, capabilities, analytics_disabled, product_feedback_disabled, uuid, memory_paths, messaging_socket_path, fast_mode_state, fast_mode_disabled_reason`. The planning session additionally measured no `remote` substring anywhere in the stream, no state written under `~/.claude`, and no session registered at account level. | Trap T1's shape precisely: accepts the flag, exits 0, does nothing observable. A day spent building a control channel on top of a no-op, with every log line looking healthy. The secondary risk is the opposite one — concluding from "`--remote-control` is a no-op" that the CLI has NO control surface, when `system/init` hands you a path to one on every single run. | `--input-format stream-json` plus a write to the child's stdin is the channel this project uses; `--remote-control` has no meaning under `--print` and must not be re-tried. **OPEN LEAD, recorded rather than acted on:** `messaging_socket_path` was `/tmp/cc-socks/57025.sock` (the pid), and `/tmp/cc-socks` is `drwx------` with the sockets `srw-------`, i.e. the CLI already runs a per-session Unix socket at the same trust level this project's own `say.sock` uses. Its protocol was NOT probed and nothing here depends on it. Anyone tempted to reach for `--remote-control` again should probe THAT instead — and should assume nothing about it until they have. The key list above is recorded verbatim so the next reader can check in one grep rather than one paid probe run. | quick 260907-voh |
| T116 | **An allowlist that under-grants is invisible in every signal this system emits.** `ALLOWED_TOOLS` was `['Write','Edit','Bash']`; on COD-7 -> `dzfweb/miracle-shop` (2026-09-08, CLI 2.1.263) the spawned agent was refused `Skill` and `Read`, both `decision_reason_type: "mode"` — *"Permission to use Skill has been denied because Claude Code is running in don't ask mode."* It substituted `head`/`cat` through `Bash` and shipped a correct PR anyway. | Exit 0, PR opened, Linear comment posted, `npm run verify` green — **every signal the system produces said success** while the agent was being refused the tool GSD's own skills run on. Found only by counting `system/permission_denied` in the run's own JSONL. There is no gate, no log line and no exit code that would ever have shown this; a narrower grant just makes the product quietly do less. | The allowlist is only ever established by a LIVE probe under the product's own `buildChildEnv` (`scripts/probe-gsd-allowlist.ts`) — an allowlist unit test pins the constant's VALUE and can never establish its SUFFICIENCY. Widened to `Write Edit Bash Read Skill Task`, verified at zero denials 2026-09-08. And the reason this must be re-run after every CLI upgrade: `--allowedTools` accepts an UNKNOWN name silently (measured — no parse error, unlike `--max-turns notanumber`), so a vendor-side rename narrows the grant without a single error anywhere. | quick 260908-bms |
| T117 | **The diagnostic crashed while printing its own conclusion, and had never been run.** `scripts/probe-gsd-allowlist.ts` existed for exactly this question and nothing had ever executed it. When it finally was, it named one denied tool and then died: `JSON.stringify(undefined)` returns the VALUE `undefined`, so `.slice(0, 300)` threw a TypeError at `:183`. | The `suggested:` line at `:185` — the ONE line the whole script exists to produce — was never reached, so the run that could have answered the question instead answered nothing. The same root cause hid a second defect: the two denial sources (`system/permission_denied` events and `result.permission_denials[]`) carry DIFFERENT fields for the SAME `tool_use_id` (the event has `decision_reason_type` and no `tool_input`; the result has `tool_input` and no `decision_reason_type`), and the probe pushed both into one array — so it reported `denials: 2` for one distinct refusal. | An instrument needs its own FAILURE path exercised, not just its happy path: the repaired probe was deliberately run once against the un-widened allowlist, on real data, before anything was widened. And where the product already owns a rule, the instrument calls it rather than re-deriving it — the rule moved to `pickDenials()` in `event-router.ts`, called by both `supervisor.ts` and the probe. Made a UNION keyed on `tool_use_id` rather than a source preference: preferring either source silently discards the diagnostic field only the other one carries. | quick 260908-bms |
| T118 | **A disambiguation prompt that prints a field which is not a target is a dead end.** `listing()` rendered `issueKey repoSlug state` while `matches()` accepted only an exact id, a case-insensitive issue key, or a 4+ char id prefix. One ticket mapped to two repos (`config.ts:50` allows it; `status.ts:43-48` documents it) is two ACTIVE runs sharing a key, so every printed line led with the SAME token and retyping it reproduced the identical error: ``  `COD-9` matches 2 runs — name one:`` twice over. `law status` never prints a run id for a run that has a key (`status.ts:50-56`), so the one accepted form the listing omitted was unobtainable anywhere in the product. | The `:27` doc comment claimed the output was "typeable straight back in", and it was false in exactly the case disambiguation exists for. Latent on this machine because all four mappings hold one repo; it lights up on the first multi-repo project — a shape the product advertises. **Why it hid: the two-repo sibling fixture ALREADY existed** (`resolve-run.test.ts:124-134`, two repos, one key, both active) and asserted only that both slugs appeared in the error. The construction was there; the property was not. A test that checks what an error SAYS cannot see that the error is unusable as INPUT. | Assert the round trip, not the format: split the error on newlines, take each line's first token, feed it back through the resolver, and require exactly one run per token and different runs for different lines. Observed RED at HEAD with the token quoted in the message — ``the listing printed `COD-9`; feeding it back gave: `COD-9` matches 2 runs``. Then hold the two sides together BY CONSTRUCTION rather than by agreement: `runTarget()` grows the token until `matches()` itself accepts exactly one run, so uniqueness is decided by the matcher and not by a parallel scan of one field. One `MIN_PREFIX`, one emitter. Falsified by emitting a `MIN_PREFIX - 1` prefix: ``no run matching `a1b```. And the second copy of the rule was one line away — `say.ts:80` built its `law watch` suggestion from its own `issueKey ?? id.slice(0, 8)` and suggested the ambiguous key; it now reads `resolved.target`. | quick 260908-crx |
| T119 | **A renderer with optional fields, correct logic and passing unit tests is indistinguishable from a wired one.** `renderPrBody` handled `ticketIdentifier`/`ticketUrl` correctly and `deliver.test.ts` passed them for a whole milestone. `grep` for those names hit the renderer and the test — no production caller. Both `deliver()` call sites passed `{title, body}`, so `deliver.ts`'s `o.prBody ?? { summary: o.body }` fallback made the unwired case render as a valid PR. | Every PR shipped saying `(no ticket recorded)`, `none configured`, `**not run**` — a body that reads like a considered report of nothing. Making the field required is not enough: every field of `PrBodyInput` is optional, so `prBody: {}` still compiles and still renders the defect. | Type the port so the fields that carry the fix are required — `Omit<PrBodyInput,'ciPaths'> & { ticketIdentifier: string; ticketUrl: string; summary: string }` — and delete the `??` fallback so `tsc` is the gate. An optional parameter is a dead parameter until something in the gate proves a caller sets it. | quick 260909-lvl |
| T120 | **A marker chosen to be invisible in one renderer is not invisible in another, and the gate renders nothing so it cannot tell.** `BOT_COMMENT_MARKER_PREFIX` was `<!-- law-bot` — an HTML comment, invisible on GitHub. Linear has NO HTML-comment rule: measured by posting six candidate bodies and reading back `bodyData`, a properly closed `<!-- law-bot -->` arrives as `{"type":"text","text":"<!-- law-bot -->"}`, identical to the unclosed form. Closing it changes nothing. | The operator saw `<!-- law-bot` as the first line of every bot comment for the whole milestone. `[](#law-bot)` and pure zero-width hide the text but leave a visible empty paragraph. | `[//]: # (law-bot)` — a CommonMark link reference definition, absent from `bodyData` entirely while the raw `body` round-trips byte-identical so the loop guard still matches. It must begin a block, so the writer needs a blank line after it. Do not reason about a foreign renderer; post to it and read back what it parsed. | quick 260909-lvl |
| T121 | **A path constant nothing writes to is the same defect as a parameter nothing sets.** `run-engine.ts` built `~/.linear-auto-worker/logs/<id>.log` from `LOG_DIR` while the run log was written to `~/.linear-auto-worker/runs/<id>.jsonl`. The `logs` directory did not exist on the operator's machine. | The failure-diagnosis comment quoted that fictional path to the operator on every failed run, all milestone. Wiring it into the PR body would have printed the fabrication in a second place. | One derivation (`daemonDirOf`), `LOG_DIR` and `ConfigPaths.logDir` deleted. Safe to delete because `ConfigPaths` is a pure derivation from `homedir()`, never parsed from `config.json`, so no existing operator config names it. | quick 260909-lvl |
| T122 | **A dead module whose doc comment reads exactly like the live one.** `src/ingress/poll.ts` declares `pollForMissedWork` and has had NO production caller since it was written — `grep -rn 'pollForMissedWork' src/ scripts/ \| grep -v '\.test\.'` returns two lines: its own declaration at `:74`, and a doc-comment mention in `recovery.ts:49`. The live poll is `reconcile()` (`recovery.ts:242`), aliased `sweepMissedWork` in `daemon.ts:59`, called at boot (`:711`) and on every tick (`:860`). | The module's header describes itself as the reconciliation poll and defers only the *timer* to a later phase — so a reader takes it for the live trigger and reasons about the WRONG QUERY. The brief that commissioned this task did exactly that, and its central citation was to code nothing calls. The two queries differ in what they filter on, so a plan built on the dead one designs around constraints that do not exist. | When two modules claim one job, find the CALLER before reading either header. And per T102 a dead export is not automatically a missing wire: this one is REPORTED, not deleted — deleting a module is a bigger decision than a knob-adding task should make on its own. | quick 260909-nh6 |
| T123 | **A toggle the wizard writes, surfaces in its own review prompt, and nothing consults.** `postLinearComments` reached six layers as a schema field — `types.ts:204`, `infra/config.ts:21`, four sites in `config-writer.ts`, `fakes.ts`, `daemon-fixture.ts` — and had exactly ONE mention in production code: a doc comment in `notify/notifier.ts:45` referring to `linear-channel.ts`, which contains no `createComment` call and writes no comment. Setting it to `false` changed nothing observable anywhere. | An operator sets it false to make the bot quiet on a workspace his colleagues can see, and the bot keeps commenting — with `law setup` reporting success, `npm run verify` green, and the review prompt cheerfully listing the toggle he just set. Worse in the specific: `questionsEnabled: false` does not mean silence either — `questions.ts:254-258` POSTS a comment announcing that the question flow is off, so the obvious second lever makes MORE noise, not less. | Gate at the CLIENT, not at the call sites: a `LinearClient` decorator applied once in the composition root, so the guard is structural and a seventh write site added later is silent by construction. Six guards is six chances to forget. And treat a toggle with no `grep` hit outside its own schema as UNIMPLEMENTED until proven otherwise — the schema field is the claim, not the evidence. | quick 260909-nh6 |
| T124 | **A second ngrok tunnel fails with a message that names the wrong cause.** `The endpoint 'https://given-relapsing-plop.ngrok-free.dev' is already online. ERR_NGROK_334`. It reads as a free-plan tunnel-COUNT limit and is nothing of the sort: it is a static-domain conflict — the account's reserved domain is already bound by the first process. `tunnel.ts:41` passes no `domain`, so the static domain comes from the ACCOUNT, not from this code. | The operator concluded a second instance was impossible on his plan and started designing around a limit that does not exist. The error's own text contains the disproof and reads past it. | Read the endpoint IN the message: it names a DOMAIN, not a quota. And note the structural escape — an instance with no ingress tunnel never meets the conflict at all, which is what makes `ingress: "poll"` a smaller change than working around the domain. | quick 260909-nh6 |
| T125 | **One run, two answers to "which branch did this fork from" — and a third riding along.** `run-engine.repoOf` and `worktreeOf` hardcoded `config.defaults.baseBranch` while `adapters.gatherEvidence` read the mapped repository's own `baseBranch`, which `config-writer.ts:196` fills from the repository's REAL default branch. Separately, `prepareWorktree` branches from `refs/remotes/<remote>/<base>` (T112) and RETURNED ONLY `{branch, path}`, throwing the ref away, so `deliver.ts:78` built `<base>..HEAD` from the bare local name. | A repository whose default branch is not `main` is branched from `main`, pushed with `--base main`, and has its commit count and its pull request diff computed against a third ref — with `runPrePushGates`' secret scan reading that same wrong file list, so a stale base WIDENS what the gates scan rather than narrowing it. And the diff-range half bit every run, single-repo included: T112 measured a clone 18 commits behind its own `origin/main`, where `main..HEAD` returns those 18 upstream commits for a run that committed nothing — `verdict.ts:175` gates on `commitCount === 0`, so the verdict declares the agent committed. Invisible on this machine only because all four live mappings and all twelve of the operator's repositories are `main`. | ONE lookup (`repoMappingFor` in `types.ts`, which imports nothing so every layer can reach it), and make the function that CHOSE the ref RETURN it — `PreparedWorktree.base`, persisted to `RepoRun.baseRef` (migration 003) by the same `store.updateRun` that already records the resolved branch. Fork point and diff range are then the same string by construction rather than by two modules agreeing. Do NOT re-resolve the ref in the second reader: a second implementation of one rule is this repo's most repeated defect (T72/T92/T96/T99/T101/T119). | quick 260909-tfm |
| T126 | **A wire-schema field the agent can return that nothing reads, sitting exactly where the next feature would reach for it.** `AgentResultSchema.changedRepos` was declared under a comment saying "present iff status === 'complete'"; the `complete` arm of `parseAgentResult` reads `summary`/`prTitle`/`prBody` only, and the `AgentResult` union has no such member. The agent could return it and it was discarded. | It is the obvious lever for "one pull request per repository the agent touched", and taking it makes delivery trust the agent's own claim about what it changed — the single thing `verdict.ts` exists to refuse, in a codebase where `claude -p` has been measured exiting 0 having been denied every edit (T1/T27). A dead field is not neutral: it is a suggestion, in the schema, at the exact seam where the wrong answer is cheapest to write. | Delete it in the same commit that builds the feature it would have been misused for, and judge from git. `deliver.ts` already runs `git diff --name-only <base>..HEAD` as its FIRST act, before the gates and long before the push, so the honest answer cost one early return and no new call — and that early return is a strict improvement on the single-repo path too, where a barren `complete` used to open an empty pull request. `additionalProperties: false` means the deletion is total: an unlisted field is one the agent physically cannot return. | quick 260909-tfm |
| T127 | **21 of 33 "repositories" were worktrees of the other 12, and the scanner could not tell.** `repo-discovery.ts`'s `hasGitEntry` asked only whether an entry NAMED `.git` existed — `readdir` gives it `withFileTypes` and the type was never consulted. Measured on the operator's own root: 33 directories bear a `.git` entry, 12 with it as a DIRECTORY (real clones), 21 as a FILE (linked worktrees), and `git rev-parse --path-format=absolute --git-common-dir` resolves all 33 to 12. | The wizard offers 33 checkboxes. Two picks that resolve to one repository share a `repoSlug`, so `mappingIndex` collides and `gatherEvidence`'s `repos.find(...)` silently picks whichever came first. Worse under the shared-session mode: `git worktree add` from a linked worktree registers in the COMMON directory, so one ticket opens several pull requests into one repository. | Consult the type `readdir` already returned. A linked worktree and a submodule both carry `.git` as a FILE and neither is a repository to map. Skip AND do not descend — the old code returned at any `.git` entry, so a worktree shadowed what was nested inside it, and descending would start surfacing clones vendored inside someone's worktree. Build the fixture with real `git worktree add`: a hand-written `.git` file passes against a rule that only matches the shape you typed. Executed against the operator's root: 33 before, 12 after. NOTE this is the `.git`-type rule, not common-dir dedupe — the two agree on this machine and are not the same rule. | quick 260909-tfm |
| T128 | **A doc comment asserting an invariant is falsified the moment a legitimate caller needs to violate it, and nothing forces a second look.** `pr-body.ts`'s doc comment read "All five DELV-03 sections... none of them ever empty" — true when written, silently false the instant a second, deliberately silent instance needed a PR body naming neither the tool nor the operator's home directory. | `tsc` and the full test suite passed with the comment already wrong in spirit; nothing catches a doc comment whose claim a new feature quietly contradicts. | Fixed in the same commit that added the third instance-level toggle (`prAttribution`, alongside `postLinearComments`/`updateLinearIssue`) — the comment now states the conditional invariant; treat an edited invariant as the same defect class as the dead-parameter/dead-export rows above it. | quick 260910-sm5 |
| T129 | **A query that hydrates every relation costs the caller nothing it reads.** `listAssignedOpenIssues` paged issues then ran `toLinearIssue` on all of them -- `Promise.all([issue.assignee, issue.project, issue.team, issue.state])` -- for a caller (`recovery.ts`'s `reconcile()`) that reads only `id` and `updatedAt`. Measured live: a page of 8 issues cost 1 request; resolving those four relations on the 8 cost 31 more -- 3.9 requests per issue for fields nobody consumed. At 12 open assigned issues that is ~48 requests/poll, ~2870/hour against Linear's 2500/hour cap; one instance ran 336 consecutive rate-limit failures with its watermark frozen for four hours, unable to self-heal, because every subsequent hour was also over budget. | The client-side `if (issue.updatedAt <= watermark) continue` ran AFTER the hydration was paid for, so the daemon paid full price for every issue it then discarded. And `src/ingress/poll.ts`'s `pollForMissedWork` -- T122's deferred dead twin, deliberately not deleted at the time because "deleting a module is a bigger decision than a knob-adding task should make on its own" -- had already gotten the query right (`updatedAt: { gt: new Date(since) }`, no `toLinearIssue` anywhere) and was never called. | Narrow the port's return type to `Pick<LinearIssue, 'id' \| 'updatedAt'>` so the type system refuses the hydration back, and push the watermark into the query as `updatedAt: { gt: new Date(since) }` -- a narrowed return type beats a comment. `pollForMissedWork` deleted outright rather than left a second time: reviving it would mean rebuilding the self-event/loop-guard sophistication `reconcile()` already carries (T107), a ninth instance of T72/T92/T96/T99/T101/T119/T125's "two implementations of one rule." The regression test asserts the REQUEST COUNT, not the result shape -- a test asserting only that the right issues come back would have passed against the broken code, which is exactly why none of the 771 existing tests caught this. | quick 260911-i3p |
