---
phase: 07-integration-daemon-lifecycle
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - package-lock.json
  - tsconfig.json
  - .planning/phases/07-integration-daemon-lifecycle/07-COMPILE-INVENTORY.md
autonomous: false
requirements: [HOOK-01, OPS-05]

must_haves:
  truths:
    - "`npm install` completes on the merged tree with no missing or conflicting dependency."
    - "Every runtime dependency in package.json is a package named in .planning/research/STACK.md at the version STACK.md pins."
    - "`npx tsc --noEmit` executes against the whole tree and its full output is captured to a committed inventory file."
    - "A human has seen and approved the merged dependency set before any package manager fetched code from a registry."
  artifacts:
    - path: package.json
      provides: "Single union dependency set at STACK.md pins, plus the canonical `verify` script established by Phase 1 (D-13)."
    - path: tsconfig.json
      provides: "One compiler config covering all of src/ and test/, ESM, TypeScript ~5.9."
    - path: .planning/phases/07-integration-daemon-lifecycle/07-COMPILE-INVENTORY.md
      provides: "The first-compile error inventory — the work list plans 02 and 03 consume."
  key_links:
    - from: package.json dependencies
      to: .planning/research/STACK.md pinned versions
      via: "Exact-match audit run before install; any package absent from STACK.md halts at the legitimacy checkpoint."
    - from: tsconfig.json include globs
      to: "src/domain, src/infra, src/ingress, src/orchestration, src/execution, src/outbound, src/cli, test"
      via: "A layer left out of include is a layer the first compile never sees."
---

<objective>
Make the merged eight-branch tree installable and compilable-in-principle, then take the
first full-tree typecheck and write down exactly what it says.

This plan does not fix a single type error. Its only job is to get from "eight worktrees
were merged" to "there is one dependency set, one compiler config, and a written list of
every mismatch the fan-out produced". Per 07-CONTEXT D-05 that list is the deferred cost
of rush mode landing, not a surprise — plans 02 and 03 work it off.

Purpose: nothing downstream in this phase can run until `npm install` succeeds and `tsc`
can be invoked. This is the unblock.
Output: reconciled `package.json` / `tsconfig.json`, a resolved lockfile, and
`07-COMPILE-INVENTORY.md`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/TRAPS.md
@.planning/phases/01-domain-contract-state-machine-schema/01-CONTEXT.md
@.planning/phases/07-integration-daemon-lifecycle/07-CONTEXT.md
@.planning/research/STACK.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Reconcile package.json, tsconfig.json and the npm scripts into one set</name>
  <precondition>Phases 1-6 are merged into this working tree — `src/domain/`, `src/infra/`, `src/ingress/`, `src/orchestration/`, `src/execution/` and `src/outbound/` all exist on disk. If any is missing, halt and report which phase did not land.</precondition>
  <files>package.json, tsconfig.json</files>
  <read_first>.planning/research/STACK.md (§ Recommended Stack, § Version Compatibility, § What NOT to Use), .planning/TRAPS.md rows T8 and T12</read_first>
  <action>
    Eight executors each edited package.json in isolation, so the merged file is either
    conflicted or the union of eight partial edits. Produce one coherent file.

    Dependencies: take the union of everything the six source layers import, and set each
    version to exactly what STACK.md pins — `@linear/sdk` at exact `93.0.1` with no range
    operator (T8: this package ships a new major roughly weekly, so a caret or tilde
    guarantees an eventual silent breaking bump), `typescript` at `~5.9` (T12: the npm
    `latest` tag now resolves to the Go-native rewrite, which has type-checking behaviour
    gaps against the ~74k-line generated declaration file this project depends on),
    `better-sqlite3` 13.0.3, `zod` 4.5.4, `execa` 10.0.1, `pino` 10.3.1, `@ngrok/ngrok`
    1.7.0, `@inquirer/prompts` 8.7.1, and `tsx` plus `pino-pretty` as dev dependencies.
    Keep `"type": "module"` — every one of these is ESM-first.

    Build the audit input for task 2 while you are here: write the sorted list of every
    dependency and devDependency name in the merged file to
    `.planning/phases/07-integration-daemon-lifecycle/07-DEP-AUDIT.md`, marking each row
    VERIFIED when the package name appears in STACK.md and ASSUMED when it does not. A
    package no one researched is a package a parallel executor invented under time
    pressure; that is precisely what the next task exists to catch.

    tsconfig.json: one config, ESM module resolution, `strict`, no emit for the check
    script. Its include globs must cover every layer directory plus `test/` and
    `scripts/`. A layer missing from include is a layer the first compile silently never
    reads, which would make this whole plan produce a clean-looking lie.

    Scripts: keep Phase 1's canonical `verify` script (01-CONTEXT D-13) intact —
    `tsc --noEmit && node --test`. Plan 06 extends it; do not change its shape here.
    Do not add a bundler. Do not add a linter (01-CONTEXT deferred it explicitly).
  </action>
  <verify>
    <automated>node -e "const p=JSON.parse(require('fs').readFileSync('package.json','utf8')); const d={...p.dependencies,...p.devDependencies}; if(p.type!=='module') throw new Error('not ESM'); if(d['@linear/sdk']!=='93.0.1') throw new Error('linear sdk must be an exact pin: '+d['@linear/sdk']); if(!/^~5\.9/.test(d.typescript||'')) throw new Error('typescript must be ~5.9: '+d.typescript); if(!p.scripts||!p.scripts.verify) throw new Error('verify script missing'); console.log('ok')"</automated>
    <automated>node -e "const t=require('fs').readFileSync('tsconfig.json','utf8'); for (const l of ['domain','infra','ingress','orchestration','execution','outbound','cli','test']) if(!t.includes(l)) throw new Error('tsconfig does not reach: '+l); console.log('ok')"</automated>
  </verify>
  <done>package.json parses, is ESM, pins @linear/sdk exactly and typescript to ~5.9, retains the `verify` script; tsconfig reaches every layer plus test/; 07-DEP-AUDIT.md lists every dependency as VERIFIED or ASSUMED.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <what-built>
    A single reconciled dependency set for the merged tree, audited row by row against
    `.planning/research/STACK.md` and written to
    `.planning/phases/07-integration-daemon-lifecycle/07-DEP-AUDIT.md`.

    Nothing has been fetched from a registry yet. This gate exists because eight agents
    edited package.json in parallel with no compiler and no reviewer between them, and a
    dependency that no one researched reaching `npm install` is the one failure in this
    phase that executes attacker-controlled code on the operator's machine.
  </what-built>
  <how-to-verify>
    1. `cat .planning/phases/07-integration-daemon-lifecycle/07-DEP-AUDIT.md`
    2. If every row reads VERIFIED, the merged set matches what project research actually
       installed and inspected — and TRAPS.md § Verified clean records a real scratch
       install of exactly this pinned set on 2026-09-06 (87 packages, every pin resolving
       as written). Approve.
    3. For each row reading ASSUMED, open `https://www.npmjs.com/package/<name>` and
       check: real publish history (not a single version published this week), a
       repository link that resolves, a weekly download count consistent with a package
       this project would plausibly depend on. Then decide whether it should be in the
       tree at all — in most cases the right answer is to delete it and let plan 03
       remove the import that wanted it.
    4. `git diff HEAD -- package.json` to see what the reconciliation changed.
  </how-to-verify>
  <resume-signal>Type "approved" to allow the install, or name the packages to remove</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Install once, then take the first full-tree typecheck and write it down</name>
  <files>package-lock.json, .planning/phases/07-integration-daemon-lifecycle/07-COMPILE-INVENTORY.md</files>
  <action>
    Delete any lockfile fragments the merge produced and run `npm install` once against
    the reconciled package.json, so the lockfile is generated rather than merged. Confirm
    `better-sqlite3` and `@ngrok/ngrok` resolved prebuilt binaries rather than attempting
    a native compile — both ship platform prebuilds and a compile step here means the
    wrong version got pinned.

    Then run `npx tsc --noEmit` over the whole tree and capture the complete output.
    Expect a large number of errors. That is the outcome 07-CONTEXT D-05 predicted: six
    layers were written against binding contract text rather than against compiled code,
    and this is the first moment any of it is checked. Do not fix anything in this task.

    Write `07-COMPILE-INVENTORY.md` containing: the raw error count, then the errors
    grouped by the source directory they occur in (domain / infra / ingress /
    orchestration / execution / outbound / test), then a short triage column per group
    marking each cluster as one of — MISSING-MEMBER (a call site expects a port method or
    type member that `src/domain/` does not declare), SHAPE-DRIFT (the member exists but
    its signature disagrees), or LOCAL (a plain mistake confined to one layer).
    MISSING-MEMBER clusters are plan 02's input; SHAPE-DRIFT and LOCAL are plan 03's.

    Commit the inventory. It is the only attribution any of these errors will ever get —
    the merge erased which phase produced which mismatch.
  </action>
  <verify>
    <automated>test -d node_modules/better-sqlite3 && test -d node_modules/@ngrok/ngrok && node -e "require('better-sqlite3'); console.log('native deps load')"</automated>
    <automated>npx tsc --noEmit > /tmp/law-tsc-first.txt 2>&1 || true; test -s .planning/phases/07-integration-daemon-lifecycle/07-COMPILE-INVENTORY.md && grep -qiE 'missing-member|shape-drift|local' .planning/phases/07-integration-daemon-lifecycle/07-COMPILE-INVENTORY.md && echo "inventory triaged"</automated>
  </verify>
  <done>`npm install` succeeded, both native dependencies load from prebuilds, and 07-COMPILE-INVENTORY.md holds the full first-compile output grouped by layer and triaged into MISSING-MEMBER / SHAPE-DRIFT / LOCAL clusters.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry → local tree | `npm install` executes install scripts from packages eight unreviewed agents named |
| merged branches → main | Six parallel executors wrote code no compiler or reviewer has seen |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-SC | Tampering | `npm install` on the merged package.json | high | mitigate | Task 1 audits every dependency name against STACK.md; task 2 is a blocking human gate on the ASSUMED rows; install runs only after approval |
| T-07-01 | Tampering | `@linear/sdk` version range | high | mitigate | Exact pin `93.0.1` asserted by an automated check (T8: new major roughly weekly, so any range is a scheduled breakage) |
| T-07-02 | Tampering | `typescript` version | medium | mitigate | Pin `~5.9`; `latest` resolves to the Go-native rewrite whose behaviour against the generated `.d.ts` is unverified (T12) |
| T-07-03 | Information disclosure | Committed lockfile / inventory | low | accept | Neither contains credentials; `.env` and `~/.linear-auto-worker/` are outside the repo by Phase 1 D-06/D-08 |
</threat_model>

<verification>
- `npm install` exits 0 and `node_modules/` contains prebuilt binaries for both native dependencies.
- `npx tsc --noEmit` runs to completion (non-zero exit is expected and acceptable at this plan).
- `07-COMPILE-INVENTORY.md` and `07-DEP-AUDIT.md` are committed.
</verification>

<success_criteria>
The tree has exactly one dependency set at researched pins, one compiler config that
reaches every layer, and a written, triaged inventory of every disagreement the parallel
fan-out produced.
</success_criteria>

<output>
Create `.planning/phases/07-integration-daemon-lifecycle/07-01-SUMMARY.md` when done.
Record the error count and the MISSING-MEMBER cluster list prominently — plan 02 reads it.
</output>
